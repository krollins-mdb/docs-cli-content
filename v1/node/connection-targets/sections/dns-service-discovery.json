{
  "heading": "DNS Service Discovery",
  "headingLevel": 2,
  "body": "To use DNS service discovery to look up the DNS SRV record of the service you're connecting to,\nspecify the SRV connection format in your connection string. If you specify this format,\nthe Node.js driver automatically rescans for new hosts. Your deployment can add hosts to its\ntopology without requiring changes in your client configuration.\n\nThe following code shows a connection string that uses the SRV connection format:\n\n```javascript\nconst uri = \"mongodb+srv://<hostname>/\"\n```\n\nTo learn more about the SRV connection format, see the [SRV Connection Format](manual/reference/connection-string-formats)\nentry in the MongoDB Server manual.",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "const uri = \"mongodb+srv://<hostname>/\"",
      "tokenEstimate": 12
    }
  ]
}
