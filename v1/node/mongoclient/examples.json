{
  "path": "node/mongoclient",
  "product": "node",
  "title": "Create a MongoClient",
  "summary": {
    "total": 2,
    "byLanguage": {
      "javascript": 1,
      "js": 1
    },
    "bySection": [
      {
        "heading": "Atlas Connection Example",
        "blocks": [
          {
            "language": "javascript",
            "tokenEstimate": 205
          }
        ]
      },
      {
        "heading": "Prevent a Slow Operation From Delaying Other Operations",
        "blocks": [
          {
            "language": "js",
            "tokenEstimate": 15
          }
        ]
      }
    ]
  },
  "codeBlocks": [
    {
      "language": "javascript",
      "section": "Atlas Connection Example",
      "content": "const { MongoClient, ServerApiVersion } = require('mongodb');\n\n// Replace the placeholder with your Atlas connection string\nconst uri = '<connection-string>';\n\n// Create a MongoClient with a MongoClientOptions object to set the Stable API version\nconst client = new MongoClient(uri, {\n  serverApi: {\n    version: ServerApiVersion.v1,\n    strict: true,\n    deprecationErrors: true,\n  },\n});\n\nexport async function runStableAPIConnect() {\n  try {\n    // Connect the client to the server (optional starting in v4.7)\n    await client.connect();\n\n    // Send a ping to confirm a successful connection\n    const result = await client.db('admin').command({ ping: 1 });\n    console.log(\n      'Pinged your deployment. You successfully connected to MongoDB!'\n    );\n    return result;\n  } finally {\n    // Ensures that the client will close when you finish/error\n    await client.close();\n  }\n}\nrunStableAPIConnect().catch(console.dir);\n",
      "tokenEstimate": 205
    },
    {
      "language": "js",
      "section": "Prevent a Slow Operation From Delaying Other Operations",
      "content": "const client = new MongoClient(uri, { maxPoolSize: 150 });",
      "tokenEstimate": 15
    }
  ]
}
