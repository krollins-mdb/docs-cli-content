{
  "heading": "Read Extended JSON",
  "headingLevel": 2,
  "body": "You can read an Extended JSON string into the JavaScript value or object described\nby the string by using the `EJSON.parse()` method.\n\nThe following example shows how you can read an Extended JSON string into a\nJavaScript value or object by using the `parse()` method:\n\n**Input:**\n\n```javascript\nimport { BSON } from 'mongodb';\nconst EJSON = BSON.EJSON;\n\nconst ejsonStr = `{\n   \"foo\": [\n      { \"$numberInt\": \"1\" },\n      { \"$numberInt\": \"2\" }\n   ],\n   \"bar\": { \"hello\": \"world\" },\n   \"code\": {\n      \"$code\": \"function x() { return 1; }\",\n      \"$scope\": {}\n   },\n   \"bin\": { \"$binary\": { \"base64\": \"AQIDBA==\", \"subType\": \"00\" } }\n}`;\n\nconst doc = EJSON.parse(ejsonStr);\nconsole.log(doc);\n```\n\n**Output:**\n\n```none\n{\n  foo: [ 1, 2 ],\n  bar: { hello: 'world' },\n  code: new Code('function x() { return 1; }', {}),\n  bin: Binary.createFromBase64('AQIDBA==', 0)\n}\n```\n\n> **Note:** The driver parses the `$uuid` Extended JSON type from a string to a\n> `BsonBinary` object of binary subtype 4. For more information about `$uuid` field\n> parsing, see the\n> [Special Rules for Parsing $uuid Fields](https://github.com/mongodb/specifications/blob/master/source/extended-json/extended-json.md#special-rules-for-parsing-uuid-fields)\n> section in the extended JSON specification.",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "import { BSON } from 'mongodb';\nconst EJSON = BSON.EJSON;\n\nconst ejsonStr = `{\n   \"foo\": [\n      { \"$numberInt\": \"1\" },\n      { \"$numberInt\": \"2\" }\n   ],\n   \"bar\": { \"hello\": \"world\" },\n   \"code\": {\n      \"$code\": \"function x() { return 1; }\",\n      \"$scope\": {}\n   },\n   \"bin\": { \"$binary\": { \"base64\": \"AQIDBA==\", \"subType\": \"00\" } }\n}`;\n\nconst doc = EJSON.parse(ejsonStr);\nconsole.log(doc);",
      "tokenEstimate": 132
    },
    {
      "language": "none",
      "content": "{\n  foo: [ 1, 2 ],\n  bar: { hello: 'world' },\n  code: new Code('function x() { return 1; }', {}),\n  bin: Binary.createFromBase64('AQIDBA==', 0)\n}",
      "tokenEstimate": 55
    }
  ]
}
