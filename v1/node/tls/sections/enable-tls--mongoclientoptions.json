{
  "heading": "Enable TLS",
  "headingLevel": 2,
  "body": "A `MongoClient` instance can connect with TLS if you set `tls`\nto `true` in your `MongoClientOptions` object:\n\n```js\nconst client = new MongoClient(uri, { tls: true });\n```",
  "codeBlocks": [
    {
      "language": "js",
      "content": "const client = new MongoClient(uri, { tls: true });",
      "tokenEstimate": 12
    }
  ],
  "slug": "enable-tls--mongoclientoptions",
  "selections": {
    "tabs": "mongoclientoptions"
  },
  "variantAgnostic": false
}
