{
  "heading": "Asynchronous Iteration",
  "headingLevel": 3,
  "body": "Cursors implement the [AsyncIterator](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for-await...of) interface, which\nallows you to use cursors in `for await...of` loops:\n\n```javascript\n  const cursor = myColl.find({});\n  console.log(\"async\");\n  for await (const doc of cursor) {\n    console.log(doc);\n  }\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "  const cursor = myColl.find({});\n  console.log(\"async\");\n  for await (const doc of cursor) {\n    console.log(doc);\n  }",
      "tokenEstimate": 31
    }
  ]
}
