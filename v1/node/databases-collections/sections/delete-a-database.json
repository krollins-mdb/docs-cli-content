{
  "heading": "Delete a Database",
  "headingLevel": 2,
  "body": "You can delete a database from your cluster by calling the `dropDatabase()`\nmethod on a `Database` instance.\n\nThe following example deletes the `\"test_database\"` database:\n\n```javascript\nconst database = client.db('test_database');\nawait database.dropDatabase();\n\n```\n\n> **Warning:**&#x20;\n>\n> Dropping a Database Deletes All Data in the Database\n>\n> Dropping a database permanently deletes all collections,\n> documents, and indexes within that database.\n>\n> Drop a database only if the data in it is no longer needed.",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "const database = client.db('test_database');\nawait database.dropDatabase();\n",
      "tokenEstimate": 14
    }
  ]
}
