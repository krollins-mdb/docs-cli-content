{
  "heading": "Syntax",
  "headingLevel": 2,
  "body": "The `$lookup`\n[syntax](manual/reference/operator/aggregation/lookup) is\ndescribed in the MongoDB server manual.\n\nIn Data Federation, the `from`  field in `$lookup` has the\nfollowing alternate syntax. This allows you to specify an object that\ncontains an optional database name and a required collection name:\n\n**Equalitymatch:**\n\nEquality Match\n\n```json\n{\n  $lookup: {\n    localField: \"<fieldName>\",\n    from: <collection-to-join>|{db: <db>, coll: <collection-to-join>},\n    foreignField: \"<fieldName>\",\n    as: \"<output-array-field>\",\n  }\n}\n```\n\n**Joincondition:**\n\nConditions and Sub-Queries\n\n```json\n{\n  $lookup: {\n    from: <collection to join>|{db: <db>, coll: <collection-to-join>},\n    let: { <var_1>: <expression>, …, <var_n>: <expression> },\n    pipeline: [ <pipeline to execute on the collection to join> ],\n    as: <output array field>\n  }\n}\n```",
  "codeBlocks": [
    {
      "language": "json",
      "content": "{\n  $lookup: {\n    localField: \"<fieldName>\",\n    from: <collection-to-join>|{db: <db>, coll: <collection-to-join>},\n    foreignField: \"<fieldName>\",\n    as: \"<output-array-field>\",\n  }\n}",
      "tokenEstimate": 55
    },
    {
      "language": "json",
      "content": "{\n  $lookup: {\n    from: <collection to join>|{db: <db>, coll: <collection-to-join>},\n    let: { <var_1>: <expression>, …, <var_n>: <expression> },\n    pipeline: [ <pipeline to execute on the collection to join> ],\n    as: <output array field>\n  }\n}",
      "tokenEstimate": 77
    }
  ]
}
