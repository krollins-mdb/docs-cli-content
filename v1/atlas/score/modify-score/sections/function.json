{
  "heading": "function",
  "headingLevel": 2,
  "body": "The `function` option allows you to alter the final score of the\ndocument using a numeric field . You can specify the numeric field for\ncomputing the final score through an [expression](function-score-expression). If the final result of the function score\nis less than `0`, MongoDB Search replaces the score with `0`.\n\n> **Note:** You can use `function` inside the `score` option of the\n> [embeddedDocument](embedded-document-ref) operator's `operator`. However, for\n> function score expressions that require the `path` option, the\n> numeric field that you specify as the `path` for the expression\n> must be inside the [embeddedDocument](embedded-document-ref) operator's `path`.",
  "codeBlocks": [],
  "slug": "function",
  "variantAgnostic": true
}
