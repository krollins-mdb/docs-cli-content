{
  "heading": "Minimize Additional MQL Aggregation Stages",
  "headingLevel": 2,
  "body": "Try to encapsulate the entire search logic within the `$search`\nstage itself and minimize using additional blocking stages, such as\n[$group](manual/reference/operator/aggregation/group), [$count](manual/reference/operator/aggregation/count), [$match](manual/reference/operator/aggregation/match), or\n[$sort](manual/reference/operator/aggregation/sort). This optimizes the MongoDB Search index usage, and\nreduces the need for additional database operations in `mongod`.\n\nFor queries that require multiple filtering operations, use the\n[compound](compound-ref) operator with `filter` clauses. If you must use\nthe [$match](manual/reference/operator/aggregation/match) stage in your aggregation pipeline, consider\nusing the [storedSource](fts-stored-source-definition) option to\nstore only the fields that your [$match](manual/reference/operator/aggregation/match) condition needs. You\ncan then use the [$search](pipe.$search) [Return Stored Source Fields](fts-return-stored-source-option) option to retrieve stored fields\nand avoid the `mongod` full document lookup.\n\n> **See Also:**&#x20;\n>\n> - [Storing Source Fields](perf-ref-stored-source)\n>\n> - [$sort Example](fts-return-stored-source-egs)\n\nIf you use [$group](manual/reference/operator/aggregation/group) to get basic counts for field\naggregations, you can use [faceted navigation](fts-facet-ref) inside the\n[$search](pipe.$search) stage. If you need only metadata results, you can\nuse [faceted navigation](fts-facet-ref) inside inside the [$searchMeta](pipe.$searchMeta)\nstage instead.\n\nIf you use [$count](manual/reference/operator/aggregation/count) to get a count of the number of\ndocuments, we recommend that you use [count](count-ref) inside the\n[$search](pipe.$search) or [$searchMeta](pipe.$searchMeta) stage instead.\n\n- For sorting numeric, date, string, boolean, UUID, and objectID\n  fields, use the `sort` option with the [$search](pipe.$search) stage.\n  To learn more, see [sorting](sort-ref).\n\n- For sorting geo fields, use the [near](near-ref) operator.\n\n- To sort other fields, use `$sort` and [Return Stored Source Fields](fts-return-stored-source-option) fields.",
  "codeBlocks": []
}
