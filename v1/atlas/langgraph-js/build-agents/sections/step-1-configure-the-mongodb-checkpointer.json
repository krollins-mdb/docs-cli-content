{
  "heading": "Step 1: Configure the MongoDB Checkpointer.",
  "headingLevel": 3,
  "body": "Add the following code to your `agent.ts` file\nto set up a persistence layer for your agent's state:\n\n```typescript\nconst checkpointer = new MongoDBSaver({ client, dbName });\nconst app = workflow.compile({ checkpointer });\n```",
  "codeBlocks": [
    {
      "language": "typescript",
      "content": "const checkpointer = new MongoDBSaver({ client, dbName });\nconst app = workflow.compile({ checkpointer });",
      "tokenEstimate": 22
    }
  ]
}
