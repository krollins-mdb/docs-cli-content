{
  "heading": "Usage",
  "headingLevel": 3,
  "body": "```javascript\nimport { MongoDBSaver } from \"@langchain/langgraph-checkpoint-mongodb\";\nimport { MongoClient } from \"mongodb\";\n\n// Connect to your MongoDB cluster\nconst client = new MongoClient(\"<connection-string>\");\n\n// Initialize the MongoDB checkpointer\nconst checkpointer = new MongoDBSaver(client);\n\n// Instantiate the graph with the checkpointer\nconst app = graph.compile(checkpointer);\n```\n\n> **Note:**&#x20;\n>\n> - [Tutorial](langgraph-js-build-agents)\n>\n> - [LangGraph.js API Reference](https://langchain-ai.github.io/langgraphjs/reference/classes/checkpoint_mongodb.MongoDBSaver.html)\n>\n> - [npm package](https://www.npmjs.com/package/@langchain/langgraph-checkpoint-mongodb)",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "import { MongoDBSaver } from \"@langchain/langgraph-checkpoint-mongodb\";\nimport { MongoClient } from \"mongodb\";\n\n// Connect to your MongoDB cluster\nconst client = new MongoClient(\"<connection-string>\");\n\n// Initialize the MongoDB checkpointer\nconst checkpointer = new MongoDBSaver(client);\n\n// Instantiate the graph with the checkpointer\nconst app = graph.compile(checkpointer);",
      "tokenEstimate": 75
    }
  ]
}
