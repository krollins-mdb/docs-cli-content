{
  "heading": "Behavior",
  "headingLevel": 3,
  "body": "> **Note:** The following configuration options only apply when using the C++\n> driver's [single-threaded](https://mongodb.github.io/mongo-cxx-driver/mongocxx-v3/connection-pools/)\n> `mongocxx::client` class to connect to the Atlas cluster.\n\nMongoDB drivers automatically attempt server selection following\na cluster election or failover event. By default, the C++\ndriver immediately raises an error if its first attempt to select\na server fails. The following configuration settings may improve\napplication connectivity to an Atlas cluster at the expense of\nspending more time in a server selection loop:\n\n- Set [serverSelectionTryOnce](manual/reference/connection-string)\n  to `false` to direct the C++ driver to\n  perform server selection up to the time limit defined by\n  `serverSelectionTimeoutMS`.\n\n- Lower the [serverSelectionTimeoutMS](manual/reference/connection-string)\n  to `15000` from the default of `30000`. MongoDB elections\n  typically take 10 seconds, but can be as fast as 5 seconds on\n  Atlas. Setting this value to 15 seconds\n  (`15000` milliseconds) covers the upper bound of election plus\n  additional time for latency.\n\nC#\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` Free cluster or a Flex cluster,\n> you must use a C#/.Net driver version that supports\n> MongoDB 7.0 and later. For complete documentation on compatibility between the\n> C#/.Net driver and MongoDB, see the [MongoDB compatibility matrix](csharp). We recommend that you\n> upgrade to the latest version of the driver.\n\n```csharp\nvar client = new MongoClient(\"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\");\nvar database = client.GetDatabase(\"<databaseName>\");\n```\n\n| MongoDB Version       | Recommended Driver Versions                                                      |\n| --------------------- | -------------------------------------------------------------------------------- |\n| All                   | See the MongoDB compatibility matrix for the latest recommended driver versions. |\n| MongoDB 7.0 and later | Version 2.7 and later                                                            |\n\n> **Note:** Microsoft .NET Core library versions *2.1 and later*\n> support the SNI (Server Name Indication) TLS (Transport Layer Security) extension on Linux and macOS.\n> Applications using prior versions of .NET Core on these\n> platforms cannot connect to an Atlas Free cluster\n> or Flex cluster.\n>\n> To learn more this requirement, see this\n> [dotnet/corefx issue](https://github.com//dotnet/corefx/issues/9608).\n\nGo\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` Free cluster or a Flex cluster,\n> you must use a Go driver version that supports\n> MongoDB 7.0 and later. For complete documentation on compatibility between the\n> Go driver and MongoDB, see the [MongoDB compatibility matrix](go/compatibility). We recommend that you\n> upgrade to the latest version of the driver.\n\n```go\nuri := \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\"\n\nclientOpts := options.Client().ApplyURI(uri).SetTimeout(10 * time.Second)\n\nclient, err := mongo.Connect(clientOpts)\nif err != nil {\n   log.Fatalf(\"Failed to connect to MongoDB: %v\", err)\n}\n\ndefer func() {\n   if err = client.Disconnect(); err != nil {\n      log.Fatalf(\"Failed to disconnect from MongoDB: %v\", err)\n   }\n}()\n\nctx := context.Background()\n\n// Ping the primary\nif err := client.Ping(ctx, readpref.Primary()); err != nil {\n   log.Fatalf(\"Failed to ping MongoDB: %v\", err)\n}\n```\n\n| MongoDB Version       | Recommended Driver Versions                                                      |\n| --------------------- | -------------------------------------------------------------------------------- |\n| All                   | See the MongoDB compatibility matrix for the latest recommended driver versions. |\n| MongoDB 7.0 and later | Version 1.0.0 and later                                                          |\n\nJava (Sync)\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` cluster or a Flex cluster, you must use\n> Java version 8 or later *and* use a Java driver version that\n> supports MongoDB 7.0 or later. To learn more about compatibility\n> between the Java driver and MongoDB, see the\n> [MongoDB compatibility matrix](java/sync/current/compatibility).\n\n```java\nMongoClientURI uri = new MongoClientURI(\n    \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\");\n\nMongoClient mongoClient = MongoClients.create(uri);\nMongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");\n```\n\n| MongoDB Version       | Recommended Driver Versions                                                      |\n| --------------------- | -------------------------------------------------------------------------------- |\n| All                   | See the MongoDB compatibility matrix for the latest recommended driver versions. |\n| MongoDB 7.0 and later | Version 3.8 and later                                                            |\n\nKotlin (Coroutine)\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` Free cluster or a Flex cluster,\n> you must use a Kotlin driver version that supports\n> MongoDB 7.0 and later. For complete documentation on compatibility between the\n> Kotlin driver and MongoDB, see the [MongoDB compatibility matrix](kotlin/coroutine/current/compatibility). We recommend that you\n> upgrade to the latest version of the driver.\n\n```kotlin\nval uri = \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\"\n\n// Construct a ServerApi instance using the ServerApi.builder() method\nval serverApi = ServerApi.builder()\n   .version(ServerApiVersion.V1)\n   .build()\nval settings = MongoClientSettings.builder()\n   .applyConnectionString(ConnectionString(uri))\n   .serverApi(serverApi)\n   .build()\n\n// Create a new client and connect to the server\nval mongoClient = MongoClient.create(settings)\nval database = mongoClient.getDatabase(\"<databaseName>\")\ntry {\n   // Send a ping to confirm a successful connection\n   val command = Document(\"ping\", BsonInt64(1))\n   val commandResult = database.runCommand(command)\n   println(\"Pinged your deployment. You successfully connected to MongoDB!\")\n}\n\ncatch (me: MongoException) {\n   System.err.println(me)\n}\n```\n\n| MongoDB Version       | Recommended Driver Versions |\n| --------------------- | --------------------------- |\n| MongoDB 7.0 and later | Version 4.10 and later      |\n\nNode.js\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` Free cluster or a Flex cluster,\n> you must use a Node.js driver version that supports\n> MongoDB 7.0 and later. For complete documentation on compatibility between the\n> Node.js driver and MongoDB, see the [MongoDB compatibility matrix](node/compatibility). We recommend that you\n> upgrade to the latest version of the driver.\n\n```javascript\nimport mongodb from 'mongodb';\n\nconst MongoClient = mongodb.MongoClient;\nconst uri = \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\";\nconst client = new MongoClient(uri);\nclient.connect(err => {\n  const collection = client.db(\"<databaseName>\").collection(\"<collectionName>\");\n  // perform actions on the collection object\n  client.close();\n});\n```\n\nTo connect to a database other than `admin` but still authenticate\nto the `admin` database, update the `database` component of the\nconnection string.\n\n```sh\nmongodb://username:password@host1:port1,...,hostN:portN/database?authSource=admin&...\n```\n\n> **Example:** The following connection string specifies the `cluster0` deployment and `test`\n> database component, and includes the `authSource=admin` option.\n>\n> ```javascript\n> var uriTestDb = \"mongodb+srv://<db_username>:<db_password>@cluster0.mongodb.net/test?ssl=true&authSource=admin&w=majority\";\n> MongoClient.connect(uriTestDb, function(err, db) {\n>    db.close();\n> });\n> ```\n\n| MongoDB Version       | Recommended Driver Versions                                                      |\n| --------------------- | -------------------------------------------------------------------------------- |\n| All                   | See the MongoDB compatibility matrix for the latest recommended driver versions. |\n| MongoDB 7.0 and later | Version 3.1 and later                                                            |\n\nPHP\n\n> **Note:**&#x20;\n>\n> To connect to an Atlas `M0` Free cluster or a Flex cluster,\n> you must use a PHP driver version that supports\n> MongoDB 7.0 and later. For complete documentation on compatibility between the\n> PHP driver and MongoDB, see the [MongoDB compatibility matrix](php-drivers). We recommend that you\n> upgrade to the latest version of the driver.\n\nThe following example uses the MongoDB PHP Library which provides a\nhigh-level abstraction around the lower-level PHP driver:\n\n```php\n$client = new MongoDB\\Client(\n    'mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority');\n\n$db = $client-><databaseName>;\n```\n\n| MongoDB Version       | Recommended Driver Versions                                                      |\n| --------------------- | -------------------------------------------------------------------------------- |\n| All                   | See the MongoDB compatibility matrix for the latest recommended driver versions. |\n| MongoDB 7.0 and later | PHPLIB 1.4 + mongodb-1.5.                                                        |",
  "codeBlocks": [
    {
      "language": "csharp",
      "content": "var client = new MongoClient(\"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\");\nvar database = client.GetDatabase(\"<databaseName>\");",
      "tokenEstimate": 44
    },
    {
      "language": "go",
      "content": "uri := \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\"\n\nclientOpts := options.Client().ApplyURI(uri).SetTimeout(10 * time.Second)\n\nclient, err := mongo.Connect(clientOpts)\nif err != nil {\n   log.Fatalf(\"Failed to connect to MongoDB: %v\", err)\n}\n\ndefer func() {\n   if err = client.Disconnect(); err != nil {\n      log.Fatalf(\"Failed to disconnect from MongoDB: %v\", err)\n   }\n}()\n\nctx := context.Background()\n\n// Ping the primary\nif err := client.Ping(ctx, readpref.Primary()); err != nil {\n   log.Fatalf(\"Failed to ping MongoDB: %v\", err)\n}",
      "tokenEstimate": 153
    },
    {
      "language": "java",
      "content": "MongoClientURI uri = new MongoClientURI(\n    \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\");\n\nMongoClient mongoClient = MongoClients.create(uri);\nMongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");",
      "tokenEstimate": 61
    },
    {
      "language": "kotlin",
      "content": "val uri = \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\"\n\n// Construct a ServerApi instance using the ServerApi.builder() method\nval serverApi = ServerApi.builder()\n   .version(ServerApiVersion.V1)\n   .build()\nval settings = MongoClientSettings.builder()\n   .applyConnectionString(ConnectionString(uri))\n   .serverApi(serverApi)\n   .build()\n\n// Create a new client and connect to the server\nval mongoClient = MongoClient.create(settings)\nval database = mongoClient.getDatabase(\"<databaseName>\")\ntry {\n   // Send a ping to confirm a successful connection\n   val command = Document(\"ping\", BsonInt64(1))\n   val commandResult = database.runCommand(command)\n   println(\"Pinged your deployment. You successfully connected to MongoDB!\")\n}\n\ncatch (me: MongoException) {\n   System.err.println(me)\n}",
      "tokenEstimate": 190
    },
    {
      "language": "javascript",
      "content": "import mongodb from 'mongodb';\n\nconst MongoClient = mongodb.MongoClient;\nconst uri = \"mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority\";\nconst client = new MongoClient(uri);\nclient.connect(err => {\n  const collection = client.db(\"<databaseName>\").collection(\"<collectionName>\");\n  // perform actions on the collection object\n  client.close();\n});",
      "tokenEstimate": 85
    },
    {
      "language": "sh",
      "content": "mongodb://username:password@host1:port1,...,hostN:portN/database?authSource=admin&...",
      "tokenEstimate": 25
    },
    {
      "language": "javascript",
      "content": "var uriTestDb = \"mongodb+srv://<db_username>:<db_password>@cluster0.mongodb.net/test?ssl=true&authSource=admin&w=majority\";\nMongoClient.connect(uriTestDb, function(err, db) {\n   db.close();\n});",
      "tokenEstimate": 53
    },
    {
      "language": "php",
      "content": "$client = new MongoDB\\Client(\n    'mongodb+srv://<db_username>:<db_password>@<clusterName>.mongodb.net/?retryWrites=true&w=majority');\n\n$db = $client-><databaseName>;",
      "tokenEstimate": 44
    }
  ],
  "slug": "behavior--cpp",
  "selections": {
    "drivers": "cpp"
  },
  "variantAgnostic": false
}
