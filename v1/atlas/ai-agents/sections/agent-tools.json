{
  "heading": "Agent Tools",
  "headingLevel": 3,
  "body": "In the context of AI agents, a tool is anything that can be programmatically defined\nand invoked by the agent. Tools extend the agent's capabilities beyond generating text,\nallowing it to interact with external systems, retrieve information, and take actions.\nTools are typically defined with a specific interface that includes:\n\n- A name and description that help the agent understand when to use the tool.\n\n- Required parameters and their expected formats.\n\n- A function that performs the actual operation when invoked.\n\nThe agent uses its reasoning capabilities to determine which tool to use, when to use it,\nand what parameters to provide, based on the user's input and the task at hand.\n\nIn addition to standard MongoDB queries, MongoDB provides several\nsearch capabilities that you can implement as tools for your agent.\n\n- **MongoDB Vector Search**: Perform vector search to retrieve relevant context based on\n  semantic meaning and similarity. To learn more, see [MongoDB Vector Search](avs-overview).\n\n- **MongoDB Search**: Perform full-text search to retrieve relevant context based on\n  keyword matching and relevance scoring. To learn more, see\n  [MongoDB Search](atlas-search).\n\n- **Hybrid Search**: Combine MongoDB Vector Search with MongoDB Search to leverage the strengths of\n  both approaches. To learn more, see [How to Perform Hybrid Search](as_hybrid-search).\n\nYou can define tools manually or by using [MongoDB AI Integrations](ai-integrations)\nsuch as LangChain and LangGraph, which provide built-in abstractions for tool\ncreation and calling.\n\nTool definition\n\nNode.js\n\n```javascript\n async function vectorSearchTool(query) {\n    const pipeline = [\n        {\n            $vectorSearch: {\n                // Vector search query pipeline...\n            }\n        }\n    ];\n\n    const results = await collection.aggregate(pipeline).toArray();\n    return results;\n}\n```\n\nPython\n\n```python\ndef vector_search_tool(query: str) -> str:\n   pipeline = [\n       {\n           \"$vectorSearch\": {\n               # Vector search query pipeline...\n           }\n       }\n   ]\n   results = collection.aggregate(pipeline)\n\n   array_of_results = []\n   for doc in results:\n       array_of_results.append(doc)\n   return array_of_results\n```\n\nTool call",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": " async function vectorSearchTool(query) {\n    const pipeline = [\n        {\n            $vectorSearch: {\n                // Vector search query pipeline...\n            }\n        }\n    ];\n\n    const results = await collection.aggregate(pipeline).toArray();\n    return results;\n}",
      "tokenEstimate": 51
    },
    {
      "language": "python",
      "content": "def vector_search_tool(query: str) -> str:\n   pipeline = [\n       {\n           \"$vectorSearch\": {\n               # Vector search query pipeline...\n           }\n       }\n   ]\n   results = collection.aggregate(pipeline)\n\n   array_of_results = []\n   for doc in results:\n       array_of_results.append(doc)\n   return array_of_results",
      "tokenEstimate": 68
    }
  ],
  "slug": "agent-tools",
  "variantAgnostic": true
}
