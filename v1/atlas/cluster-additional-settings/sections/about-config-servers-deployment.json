{
  "heading": "About Config Servers Deployment",
  "headingLevel": 3,
  "body": "For dedicated config servers, Atlas deploys the [config servers](manual/core/sharded-cluster-config-servers)\nas a three-node replica set. The config servers run on M30 cluster\ntiers. In multi-region clusters, config servers are distributed\nacross regions.\n\nFor cross-region clusters, Atlas distributes the config server\nreplica set nodes to ensure optimal availability. For example,\nAtlas might deploy the config servers across three distinct\navailability zones and three distinct regions if supported by\nthe selected cloud service provider and region configuration. Config\nservers must remain readable for sharded read operations to continue to\nfunction. To learn more, see [Config Server Availability](manual/core/sharded-cluster-config-servers).\n\nIf you enable Atlas-managed config servers, Atlas may colocate config server\ndata with application data instead of using a dedicated config server.\nTo learn more, see [Atlas-Managed Config Servers for Sharded Clusters](config-server-management).\n\nA regional outage or regional outage simulation that affects the\nhighest priority regions in a sharded cluster could cause the\ncluster to become inoperable for read operations. To restore the config\nservers, do the following:\n\n- Configure a [read preference](manual/core/read-preference) that is\n  suitable for querying secondary nodes for reads.\n\n- [Reconfigure a Replica Set During a Regional Outage](reconfigure-rs-during-regional-outage) the\n  cluster for regaining electable nodes.",
  "codeBlocks": []
}
