{
  "heading": "View MongoDB Search Indexes",
  "headingLevel": 2,
  "body": "To use the [.NET/C# Driver](csharp) to retrieve your MongoDB Search indexes,\nuse the `List()` or `ListAsync()` method.\n\nThe following sample application returns the indexes on a collection.\nSpecify the following values:\n\n- Your Atlas connection string. To learn more, see [Driver](connect-via-driver).\n\n- The database and collection that contains the search indexes\n  that you want to retrieve.\n\n> **Note:** The `List()` method returns a cursor instead of the indexes themselves.\n> To access the indexes, use a cursor paradigm such\n> as the `MoveNext()` method.\n\n```csharp\nusing MongoDB.Bson;\nusing MongoDB.Driver;\n\n// connect to your Atlas deployment\nvar uri = \"<connection-string>\";\n\nvar client = new MongoClient(uri);\n\nvar db = client.GetDatabase(\"<databaseName>\");\nvar collection = db.GetCollection<BsonDocument>(\"<collectionName>\");\n\n// list your MongoDB Search indexes\nvar result = collection.SearchIndexes.List().ToList();\nforeach (var index in result)\n{\n    Console.WriteLine(index);\n}\n\n```\n\nTo run the sample application, create a new .NET console project named\n`csharp-list-indexes` and copy the previous code example into the\n`Program.cs` file. Then, use the following command to run the project:\n\n**Input:**\n\n```shell\ndotnet run csharp-list-indexes.csproj\n```\n\n**Output:**\n\n```\n<indexes for this collection>\n```\n\n> **Tip:**&#x20;\n>\n> API Documentation\n>\n> To learn more about the methods on this page, see the\n> [API documentation](https://mongodb.github.io/mongo-csharp-driver/2.21/apidocs/html/Methods_T_MongoDB_Driver_Search_IMongoSearchIndexManager.htm)\n> for the .NET/C# driver.",
  "codeBlocks": [
    {
      "language": "csharp",
      "content": "using MongoDB.Bson;\nusing MongoDB.Driver;\n\n// connect to your Atlas deployment\nvar uri = \"<connection-string>\";\n\nvar client = new MongoClient(uri);\n\nvar db = client.GetDatabase(\"<databaseName>\");\nvar collection = db.GetCollection<BsonDocument>(\"<collectionName>\");\n\n// list your MongoDB Search indexes\nvar result = collection.SearchIndexes.List().ToList();\nforeach (var index in result)\n{\n    Console.WriteLine(index);\n}\n",
      "tokenEstimate": 85
    },
    {
      "language": "shell",
      "content": "dotnet run csharp-list-indexes.csproj",
      "tokenEstimate": 10
    },
    {
      "language": "text",
      "content": "<indexes for this collection>",
      "tokenEstimate": 6
    }
  ],
  "slug": "view-mongodb-search-indexes--atlas-driver-csharp",
  "selections": {
    "deployment-type": "atlas",
    "interface": "driver",
    "language": "csharp"
  },
  "variantAgnostic": false
}
