{
  "heading": "Create a MongoDB Search Index",
  "headingLevel": 2,
  "body": "To create a MongoDB Search index using the [Java Driver](java/sync/current):- Construct a document that defines the search index\n\n- Pass the document to the `createSearchIndex()` or `createSearchIndexes()` method.> **Note:** The MongoDB Search index management methods run asynchronously. The\n> driver methods can return before confirming that they ran\n> successfully. To determine the current status of the indexes, call the\n> `listSearchIndexes()` method on your collection.Create One Search IndexThe following sample application defines a search index to dynamically\nindex the fields in your collection, and then runs the `createSearchIndex()`\nmethod to create the index. To learn more, see [index definitions](ref-index-definitions).```java\nimport com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport org.bson.Document;\n\npublic class CreateIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collectionName>\");\n\n            Document index = new Document(\"mappings\",\n                    new Document(\"dynamic\", true));\n            collection.createSearchIndex(\"<index-name>\", index);\n        }\n    }\n}\n\n```Create Multiple Search IndexesYou can use the following sample application to create multiple MongoDB Search indexes\nat once. To do so, construct a document for each search index that\nyou wish to create, and then pass the documents as an array to\nthe `createSearchIndexes()` method:```java\nimport com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport com.mongodb.client.model.SearchIndexModel;\nimport org.bson.Document;\nimport java.util.Arrays;\n\npublic class CreateIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collectionName>\");\n\n            SearchIndexModel indexOne = new SearchIndexModel(\"<first-index-name>\",\n                    new Document( \n                        // search index definition fields\n                    ));\n\n            SearchIndexModel indexTwo = new SearchIndexModel(\"<second-index-name>\",\n                    new Document( \n                        // search index definition fields\n                    ));\n\n            collection.createSearchIndexes(Arrays.asList(indexOne, indexTwo));\n        }\n    }\n}\n\n```* Your Atlas connection string. To learn more, see [Driver](connect-via-driver).\n\n* The database and collection for which you want to create the index.\n\n* The names of your indexes. If you omit an index's name, MongoDB Search names the index `default`.Create One Search IndexCreate Multiple Search Indexes* The fields to define your search index. To learn more, see [index definitions](ref-index-definitions).```shell\njavac CreateIndex.java\njava CreateIndex\n```",
  "codeBlocks": [
    {
      "language": "java",
      "content": "import com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport org.bson.Document;\n\npublic class CreateIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collectionName>\");\n\n            Document index = new Document(\"mappings\",\n                    new Document(\"dynamic\", true));\n            collection.createSearchIndex(\"<index-name>\", index);\n        }\n    }\n}\n",
      "tokenEstimate": 145
    },
    {
      "language": "java",
      "content": "import com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport com.mongodb.client.model.SearchIndexModel;\nimport org.bson.Document;\nimport java.util.Arrays;\n\npublic class CreateIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<databaseName>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collectionName>\");\n\n            SearchIndexModel indexOne = new SearchIndexModel(\"<first-index-name>\",\n                    new Document( \n                        // search index definition fields\n                    ));\n\n            SearchIndexModel indexTwo = new SearchIndexModel(\"<second-index-name>\",\n                    new Document( \n                        // search index definition fields\n                    ));\n\n            collection.createSearchIndexes(Arrays.asList(indexOne, indexTwo));\n        }\n    }\n}\n",
      "tokenEstimate": 203
    },
    {
      "language": "shell",
      "content": "javac CreateIndex.java\njava CreateIndex",
      "tokenEstimate": 9
    }
  ]
}
