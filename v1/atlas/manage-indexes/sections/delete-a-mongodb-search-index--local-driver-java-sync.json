{
  "heading": "Delete a MongoDB Search Index",
  "headingLevel": 2,
  "body": "To delete a MongoDB Search index on a collection using the [Java\nDriver](java/sync/current/indexes),\nuse the `dropSearchIndex()` method:\n\nThe following sample application deletes the specified MongoDB Search index\non the specified collection.\n\n```java\nimport com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport org.bson.Document;\n\npublic class DeleteIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<database-name>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collection>\");\n            // delete the index\n            collection.dropSearchIndex(\"<index-name>\");\n        }\n    }\n}\n\n```\n\n- `<connection-string>`: Your Atlas connection string. To\n  learn more, see [Driver](connect-via-driver).\n\n  IMPORTANT: In your connection string, don't include the\n  [Write Concern](manual/reference/write-concern)\n  setting.\n\n- `<database-name>`: The name of the database that contains the\n  collection.\n\n- `<collection-name>`: The name of the collection for which you\n  want to retrieve the index.\n\n- `<index-name>`: The name of the index to delete.\n\n```shell\njavac DeleteIndex.java\njava DeleteIndex\n```\n\n> **See Also:** [MongoDB Search Indexes](java/sync/current/indexes)",
  "codeBlocks": [
    {
      "language": "java",
      "content": "import com.mongodb.client.MongoClient;\nimport com.mongodb.client.MongoClients;\nimport com.mongodb.client.MongoCollection;\nimport com.mongodb.client.MongoDatabase;\nimport org.bson.Document;\n\npublic class DeleteIndex {\n    public static void main(String[] args) {\n        // connect to your Atlas cluster\n        String uri = \"<connection-string>\";\n\n        try (MongoClient mongoClient = MongoClients.create(uri)) {\n            // set namespace\n            MongoDatabase database = mongoClient.getDatabase(\"<database-name>\");\n            MongoCollection<Document> collection = database.getCollection(\"<collection>\");\n            // delete the index\n            collection.dropSearchIndex(\"<index-name>\");\n        }\n    }\n}\n",
      "tokenEstimate": 130
    },
    {
      "language": "shell",
      "content": "javac DeleteIndex.java\njava DeleteIndex",
      "tokenEstimate": 9
    }
  ],
  "slug": "delete-a-mongodb-search-index--local-driver-java-sync",
  "selections": {
    "deployment-type": "local",
    "interface": "driver",
    "language": "java-sync"
  },
  "variantAgnostic": false
}
