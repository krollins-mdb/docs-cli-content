{
  "heading": "Create a MongoDB Search Index",
  "headingLevel": 2,
  "body": "To create a MongoDB Search index through the [Node Driver](node):\n\n- Define the search index from your application.\n\n- Run the `createSearchIndex` or `createSearchIndexes` helper method.\n\n> **Note:** The MongoDB Search index management methods run asynchronously. The\n> driver methods can return before confirming that they ran\n> successfully. To determine the current status of the indexes, call the\n> `listSearchIndexes()` method on your collection.\n\nCreate One Search Index\n\nReplace the placeholder values in the following example application named\n`create-index.js`, which uses the `createSearchIndex` command to define\na MongoDB Search index:\n\n| Value               | Description                                                                                 |\n| ------------------- | ------------------------------------------------------------------------------------------- |\n| \\<connectionString> | Your Atlas connection string. To learn more, see Connect to a Cluster via Client Libraries. |\n| \\<databaseName>     | Database for which you want to create the index.                                            |\n| \\<collectionName>   | Collection for which you want to create the index.                                          |\n| \\<indexName>        | Name of your index. If you omit the index name, MongoDB Search names the index default.     |\n| \\<IndexDefinition>  | The definition of your index. To learn about index definition syntax, see Index Reference.  |\n\n```javascript\nimport { MongoClient } from \"mongodb\";\n\n// connect to your Atlas deployment\nconst uri =  \"<connectionString>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"<indexName>\",\n        definition: {\n            /* search index definition fields */\n            <indexDefinition>\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n\n```\n\nCreate Multiple Search Indexes\n\n| Value               | Description                                                                                 |\n| ------------------- | ------------------------------------------------------------------------------------------- |\n| \\<connectionString> | Your Atlas connection string. To learn more, see Connect to a Cluster via Client Libraries. |\n| \\<databaseName>     | Database for which you want to create the index.                                            |\n| \\<collectionName>   | Collection for which you want to create the index.                                          |\n| \\<indexName>        | Name of your index. If you omit the index name, MongoDB Search names the index default.     |\n| \\<IndexDefinition>  | The definition of your index. To learn about index definition syntax, see Index Reference.  |\n\nReplace the placeholder values in the following example application named\n`create-index.js`, which uses the `createSearchIndexes` command to define\na MongoDB Search index:\n\n```javascript\nimport { MongoClient } from \"mongodb\";\n\n// connect to your Atlas deployment\nconst uri =  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n\n    // define an array of MongoDB Search indexes\n    const indexes = [\n        {\n            name: \"<first-index-name>\",\n            definition: {\n                /* search index definition fields */\n            }\n        },\n        ...\n        {\n            name: \"<last-index-name>\",\n            definition: {\n                /* search index definition fields */\n            }\n        }\n    ]\n    \n    // run the helper method\n    const result = await collection.createSearchIndexes(indexes);\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n\n```\n\nUse the following command:\n\n**Input:**\n\n```shell\nnode create-index.js\n```\n\n**Output:**\n\n```\n<index-name>\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "import { MongoClient } from \"mongodb\";\n\n// connect to your Atlas deployment\nconst uri =  \"<connectionString>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"<indexName>\",\n        definition: {\n            /* search index definition fields */\n            <indexDefinition>\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n",
      "tokenEstimate": 140
    },
    {
      "language": "javascript",
      "content": "import { MongoClient } from \"mongodb\";\n\n// connect to your Atlas deployment\nconst uri =  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n\n    // define an array of MongoDB Search indexes\n    const indexes = [\n        {\n            name: \"<first-index-name>\",\n            definition: {\n                /* search index definition fields */\n            }\n        },\n        ...\n        {\n            name: \"<last-index-name>\",\n            definition: {\n                /* search index definition fields */\n            }\n        }\n    ]\n    \n    // run the helper method\n    const result = await collection.createSearchIndexes(indexes);\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n",
      "tokenEstimate": 170
    },
    {
      "language": "shell",
      "content": "node create-index.js",
      "tokenEstimate": 4
    },
    {
      "language": "text",
      "content": "<index-name>",
      "tokenEstimate": 4
    }
  ],
  "slug": "create-a-mongodb-search-index--self-driver-nodejs",
  "selections": {
    "deployment-type": "self",
    "interface": "driver",
    "language": "nodejs"
  },
  "variantAgnostic": false
}
