{
  "heading": "How often does an Atlas Cluster rotate TLS certificates?",
  "headingLevel": 3,
  "body": "TLS (Transport Layer Security) certificates are valid for 90 days from the day it is issued.\nCertificates are rotated 42 days before the certificate's expiration\ndate.\n\nUse the following command to check expiration of your TLS (Transport Layer Security) certificate\nof a node:\n\n```\necho | openssl s_client -showcerts -connect $HOSTNAME:$PORT 2> /dev/null | openssl x509 -noout -enddate\n```",
  "codeBlocks": [
    {
      "language": "text",
      "content": "echo | openssl s_client -showcerts -connect $HOSTNAME:$PORT 2> /dev/null | openssl x509 -noout -enddate",
      "tokenEstimate": 31
    }
  ]
}
