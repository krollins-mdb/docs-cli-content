{
  "heading": "Query Examples",
  "headingLevel": 3,
  "body": "The following example searches all `title` fields for movie titles\nthat begin with `Green D`, followed by any number of other\ncharacters.\n\nAtlas UI\n\nMongoDB Shell\n\nCompass\n\nC#\n\nGo\n\nJava (Sync)\n\nKotlin (Coroutine)\n\nNode.js\n\nPython\n\nThe following example searches all `title` fields for movie titles\nthat begin with the string `Wom?n` (where `?` may be any single\ncharacter), followed by a space and then any number of additional\ncharacters.\n\nAtlas UI\n\nMongoDB Shell\n\nCompass\n\nC#\n\nGo\n\nJava (Sync)\n\nKotlin (Coroutine)\n\nNode.js\n\nPython\n\nThe following example searches using the **escape character** for documents\nin which the `title` field ends with a question mark.\n\n> **Note:** The following example is intended to run in [mongosh](mongodb-shell). For\n> more information about using the escape characters with a driver,\n> see [Escape Character Behavior](escape-character-behavior).\n\nThe `*` character in the `query` field matches any characters, and\nthe `\\\\?` string matches a literal question mark.\n\nAtlas UI\n\nMongoDB Shell\n\nCompass\n\nC#\n\nGo\n\nJava (Sync)\n\nKotlin (Coroutine)\n\nNode.js\n\nPython\n\nThe following query uses the [$searchMeta](pipe.$searchMeta) stage to search for\nmovies that contain the term `summer` anywhere in the `title` field\nand retrieve the number of movies that match the criteria in each genre.\n\n**Input:**\n\n```json\ndb.movies.aggregate([\n   {\n      \"$searchMeta\": {\n         \"facet\": {\n         \"operator\": {\n            \"wildcard\": {\n               \"path\": \"title\",\n               \"query\": \"*summer*\"\n            }\n         },\n         \"facets\": {\n            \"genresFacet\": {\n               \"type\": \"string\",\n               \"path\": \"genres\"\n            }\n         }\n         }\n      }\n   }\n])\n```\n\n**Output:**\n\n```javascript\n[\n   {\n      count: { lowerBound: Long('6') },\n      facet: {\n         genresFacet: {\n            buckets: [\n               { _id: 'Comedy', count: Long('5') },\n               { _id: 'Fantasy', count: Long('3') },\n               { _id: 'Romance', count: Long('3') },\n               { _id: 'Drama', count: Long('2') },\n               { _id: 'Horror', count: Long('1') },\n               { _id: 'Mystery', count: Long('1') }\n            ]\n         }\n      }\n   }\n]\n```",
  "codeBlocks": [
    {
      "language": "json",
      "content": "db.movies.aggregate([\n   {\n      \"$searchMeta\": {\n         \"facet\": {\n         \"operator\": {\n            \"wildcard\": {\n               \"path\": \"title\",\n               \"query\": \"*summer*\"\n            }\n         },\n         \"facets\": {\n            \"genresFacet\": {\n               \"type\": \"string\",\n               \"path\": \"genres\"\n            }\n         }\n         }\n      }\n   }\n])",
      "tokenEstimate": 85
    },
    {
      "language": "javascript",
      "content": "[\n   {\n      count: { lowerBound: Long('6') },\n      facet: {\n         genresFacet: {\n            buckets: [\n               { _id: 'Comedy', count: Long('5') },\n               { _id: 'Fantasy', count: Long('3') },\n               { _id: 'Romance', count: Long('3') },\n               { _id: 'Drama', count: Long('2') },\n               { _id: 'Horror', count: Long('1') },\n               { _id: 'Mystery', count: Long('1') }\n            ]\n         }\n      }\n   }\n]",
      "tokenEstimate": 134
    }
  ],
  "slug": "query-examples",
  "variantAgnostic": true
}
