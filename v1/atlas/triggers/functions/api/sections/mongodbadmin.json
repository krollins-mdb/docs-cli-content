{
  "heading": "mongodb.admin()",
  "headingLevel": 2,
  "body": "Gets a handle for the `admin` database in a linked MongoDB data\nsource. You can use this to run MongoDB admin commands like\n[admin.getDBNames()](admin.getDBNames).\n\n```javascript\nconst mongodb = context.services.get(\"mongodb-atlas\");\nconst admin = mongodb.admin();\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "const mongodb = context.services.get(\"mongodb-atlas\");\nconst admin = mongodb.admin();",
      "tokenEstimate": 17
    }
  ]
}
