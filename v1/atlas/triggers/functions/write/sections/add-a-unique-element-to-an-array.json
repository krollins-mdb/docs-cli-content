{
  "heading": "Add a Unique Element to an Array",
  "headingLevel": 4,
  "body": "You can use the [$addToSet](manual/reference/operator/update/addToSet) operator to add a value to an\narray field if that value is not already included in the array. If the\nvalue is already present, `$addToSet` does nothing.\n\n```javascript\n{ \"$addToSet\": { \"<Array Field Name>\": <Potentially Unique Value>, ... } }\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "{ \"$addToSet\": { \"<Array Field Name>\": <Potentially Unique Value>, ... } }",
      "tokenEstimate": 21
    }
  ]
}
