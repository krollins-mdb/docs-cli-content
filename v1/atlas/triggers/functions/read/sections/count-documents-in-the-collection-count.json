{
  "heading": "Count Documents in the Collection (count())",
  "headingLevel": 3,
  "body": "You can count documents in a collection using the\n[collection.count()](collection.count) method. You can specify a query to\ncontrol which documents to count. If you don't specify a query, the\nmethod counts all documents in the collection.\n\nThe following [Atlas Function](atlas-functions) snippet counts the number of\ndocuments in the `items` collection that have at least one review:\n\n```javascript\nreturn itemsCollection.count({ \"reviews.0\": { \"$exists\": true } })\n  .then(numDocs => console.log(`${numDocs} items have a review.`))\n  .catch(err => console.error(\"Failed to count documents: \", err))\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "return itemsCollection.count({ \"reviews.0\": { \"$exists\": true } })\n  .then(numDocs => console.log(`${numDocs} items have a review.`))\n  .catch(err => console.error(\"Failed to count documents: \", err))",
      "tokenEstimate": 51
    }
  ]
}
