{
  "path": "atlas/atlas-oplog",
  "product": "atlas",
  "title": "Oplog Access",
  "sections": [
    {
      "heading": "Add a User with Oplog Access",
      "headingLevel": 2,
      "body": "To access the oplog, a database user must have read access on\nthe `local` database. To create a user with read access on `local`:",
      "codeBlocks": []
    },
    {
      "heading": "Step 1: Add a user.",
      "headingLevel": 3,
      "body": "- It it's not already displayed, click the\n  **Database Users** tab.\n\n- Click plus **Add New Database User** and\n  enter a user name such as `oploguser`.\n\n- Click **Grant Specific Privileges** and select the\n  `read` role and the `local` database. This restricts the\n  user to read operations on the `local` database.\n\n- Enter a password and click **Add User**.",
      "codeBlocks": []
    },
    {
      "heading": "Access the Oplog",
      "headingLevel": 2,
      "body": "- Connect to your cluster with the\n  [mongosh](mongodb-shell), using the credentials of the new database user with\n  access to the `local` database.\n\n- Switch to the `local` database.\n\n  ```none\n  > use local\n  ```\n\n- The oplog collection is named `oplog.rs`. Database write operations\n  are recorded in date order, with a [timestamp](manual/reference/bson-types)\n  field and a wall clock field.\n\n  The timestamp field contains an integer with\n  [seconds since epoch](https://en.wikipedia.org/wiki/Unix_time).\n\n> **Note:**&#x20;\n>\n> - To increase the size of an oplog for a cluster, see\n>   [Set Oplog Size](set-oplog-size).\n>\n> - You can't use the MongoDB command\n>   [replSetResizeOplog](paid-tier-command-limitations)\n>   to resize the oplog.",
      "codeBlocks": [
        {
          "language": "none",
          "content": "> use local",
          "tokenEstimate": 3
        }
      ]
    }
  ]
}
