{
  "heading": "Run a Case-Sensitive Partial Match Query",
  "headingLevel": 3,
  "body": "autocomplete\n\nThe following query searches for documents where the `title` field\ncontains words that begin with `Great`.\n\n```c\n#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *limit_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *autocomplete_spec = bson_new();\n    \n    BSON_APPEND_UTF8(autocomplete_spec, \"query\", \"Great\");\n    BSON_APPEND_UTF8(autocomplete_spec, \"path\", \"title\");\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial-autocomplete\");\n    BSON_APPEND_DOCUMENT(search_spec, \"autocomplete\", autocomplete_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $limit stage\n    limit_stage = bson_new();\n    BSON_APPEND_INT32(limit_stage, \"$limit\", 10);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", limit_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"2\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running autocomplete search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(limit_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(autocomplete_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Autocomplete query completed\\n\");\n    return EXIT_SUCCESS;\n}\n\n```\n\nphrase\n\nThe following query searches for documents where the `plot` field\ncontains the phrase `new york` with a maximum distance of 5 between terms.\n\n```c\n#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *phrase_spec = bson_new();\n    \n    BSON_APPEND_UTF8(phrase_spec, \"path\", \"plot\");\n    BSON_APPEND_UTF8(phrase_spec, \"query\", \"new york\");\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"phrase\", phrase_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running phrase search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(phrase_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Phrase query completed\\n\");\n    return EXIT_SUCCESS;\n}\n\n```\n\nregex\n\nThe following query searches for documents where the `title` field\ncontains words that match the regular expression pattern.\n\n```c\n#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *regex_spec = bson_new();\n    \n    BSON_APPEND_UTF8(regex_spec, \"path\", \"title\");\n    BSON_APPEND_UTF8(regex_spec, \"query\", \"[0-9]{2} (.){4}s\");\n    BSON_APPEND_BOOL(regex_spec, \"allowAnalyzedField\", true);\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"regex\", regex_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running regex search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(regex_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Regex query completed\\n\");\n    return EXIT_SUCCESS;\n}\n\n```\n\nwildcard\n\nThe following query searches for documents where the `title` field\ncontains words that match the wildcard pattern.\n\n```c\n#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *wildcard_spec = bson_new();\n    \n    BSON_APPEND_UTF8(wildcard_spec, \"path\", \"title\");\n    BSON_APPEND_UTF8(wildcard_spec, \"query\", \"how*\");\n    BSON_APPEND_BOOL(wildcard_spec, \"allowAnalyzedField\", true);\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"wildcard\", wildcard_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running wildcard search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(wildcard_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Wildcard query completed\\n\");\n    return EXIT_SUCCESS;\n}\n\n```\n\nEnsure that your connection string includes your database user's\ncredentials. To learn more, see [Driver](connect-via-driver).\n\n```bash\ngcc -o partial-match-tutorial-query partial-match-tutorial-query.c $(pkg-config --cflags --libs libmongoc-1.0)\n./partial-match-tutorial-query\n```\n\nThis tutorial describes how to create an index on the\n`sample_mflix.movies` collection and run partial string queries\nagainst the `plot` field using the C# driver in a local deployment.\n\nTo return matches for partial string queries, you can use one of the following operators:\n\n- The [autocomplete](autocomplete-ref) operator, which allows you to search the\n  specified fields for a word or phrase that contains the sequence of\n  characters that you specify with your query.\n\n- The [Phrase](phrase-ref) operator, which allows you to search the\n  specified fields for documents that contain the terms in your query\n  string at the distance you specify between the terms.\n\n- The [regex](regex-ref) operator, which allows you to search the\n  specified fields for strings using regular expression.\n\n- The [wildcard](wildcard-ref) operator, which allows you to search the\n  specified fields using special characters in your query to match any\n  character.\n\nYou can also use the [text](text-ref) operator with a custom analyzer\nfor more fine-grained control over partial matching. To learn more, see\n[Custom analyzers](custom-analyzers).\n\nThis tutorial takes you through the following steps:\n\n- Set up a MongoDB Search index on the `plot` field in the\n  `sample_mflix.movies` collection.\n\n- Run MongoDB Search query for a partial string against the `plot` field in\n  the `sample_mflix.movies` collection using\n  [autocomplete](autocomplete-ref), [Phrase](phrase-ref), [regex](regex-ref), and\n  [wildcard](wildcard-ref) operators.\n\nBefore you begin, ensure that your cluster meets the following requirements.\n\nTo complete the tutorials, you must have:\n\n- An Atlas cluster with MongoDB version 6.0 or higher or a MongoDB\n  self-managed cluster with MongoDB version 8.2 or higher.\n\n- The [Load Sample Data Into Atlas](sample-data) loaded into your\n  cluster.\n\n- [Project Data Access Admin](<Project Data Access Admin>) or higher access to your project to create MongoDB Search indexes.\n\n- [Atlas UI](fts-tutorial-run-query), [mongosh](mongodb-shell), [Compass](https://www.mongodb.com/try/download/compass), or a supported\n  [MongoDB Driver](drivers) to run queries on your cluster.\n\n> **Note:** You can run MongoDB Vector Search queries by using any driver\n> through the $vectorSearch aggregation stage.\n> These tutorials include examples for a selection\n> of clients. Refer to the specific tutorial page for details.\n>\n> You can also complete these tutorials with local deployments that you create\n> with the Atlas CLI or with an on-prem deployment. To learn more, see\n> [Create a Local Atlas Deployment](atlas/cli/current/atlas-cli-deploy-local) and\n> [Self-Managed Deployments](manual/self-managed-deployments).\n\nTo learn more about how to implement partial matches, see [MongoDB Text Search: Substring Pattern Matching Including Regex and Wildcard, Use Search Instead](https://medium.com/mongodb/mongodb-text-search-substring-pattern-matching-including-regex-and-wildcard-use-search-instead-3633c6f7e604).",
  "codeBlocks": [
    {
      "language": "c",
      "content": "#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *limit_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *autocomplete_spec = bson_new();\n    \n    BSON_APPEND_UTF8(autocomplete_spec, \"query\", \"Great\");\n    BSON_APPEND_UTF8(autocomplete_spec, \"path\", \"title\");\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial-autocomplete\");\n    BSON_APPEND_DOCUMENT(search_spec, \"autocomplete\", autocomplete_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $limit stage\n    limit_stage = bson_new();\n    BSON_APPEND_INT32(limit_stage, \"$limit\", 10);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", limit_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"2\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running autocomplete search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(limit_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(autocomplete_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Autocomplete query completed\\n\");\n    return EXIT_SUCCESS;\n}\n",
      "tokenEstimate": 1001
    },
    {
      "language": "c",
      "content": "#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *phrase_spec = bson_new();\n    \n    BSON_APPEND_UTF8(phrase_spec, \"path\", \"plot\");\n    BSON_APPEND_UTF8(phrase_spec, \"query\", \"new york\");\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"phrase\", phrase_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running phrase search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(phrase_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Phrase query completed\\n\");\n    return EXIT_SUCCESS;\n}\n",
      "tokenEstimate": 944
    },
    {
      "language": "c",
      "content": "#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *regex_spec = bson_new();\n    \n    BSON_APPEND_UTF8(regex_spec, \"path\", \"title\");\n    BSON_APPEND_UTF8(regex_spec, \"query\", \"[0-9]{2} (.){4}s\");\n    BSON_APPEND_BOOL(regex_spec, \"allowAnalyzedField\", true);\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"regex\", regex_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running regex search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(regex_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Regex query completed\\n\");\n    return EXIT_SUCCESS;\n}\n",
      "tokenEstimate": 964
    },
    {
      "language": "c",
      "content": "#include <mongoc/mongoc.h>\n#include <bson/bson.h>\n#include <stdio.h>\n#include <stdlib.h>\n\n// MongoDB connection URI\n#define MONGODB_URI \"<connection-string>\"\n\n// Database and collection names\n#define DATABASE_NAME \"sample_mflix\"\n#define COLLECTION_NAME \"movies\"\n\nmongoc_client_t* connect_to_mongodb(void) {\n    mongoc_client_t *client;\n    mongoc_uri_t *uri;\n    bson_error_t error;\n\n    // Initialize MongoDB C driver\n    mongoc_init();\n\n    // Create a MongoDB URI object\n    uri = mongoc_uri_new_with_error(MONGODB_URI, &error);\n    if (!uri) {\n        fprintf(stderr, \"Failed to parse URI: %s\\n\", error.message);\n        return NULL;\n    }\n\n    // Create a new client instance\n    client = mongoc_client_new_from_uri(uri);\n    if (!client) {\n        fprintf(stderr, \"Failed to create MongoDB client\\n\");\n        mongoc_uri_destroy(uri);\n        return NULL;\n    }\n\n    // Configure the client for server API version\n    mongoc_client_set_error_api(client, 2);\n    \n    mongoc_uri_destroy(uri);\n    return client;\n}\n\nvoid cleanup_mongodb(mongoc_client_t *client) {\n    if (client) {\n        mongoc_client_destroy(client);\n    }\n    mongoc_cleanup();\n}\n\nvoid print_bson_document(const bson_t *doc) {\n    char *json_string = bson_as_canonical_extended_json(doc, NULL);\n    printf(\"%s\\n\", json_string);\n    bson_free(json_string);\n}\n\nint main(void) {\n    mongoc_client_t *client;\n    mongoc_database_t *database;\n    mongoc_collection_t *collection;\n    mongoc_cursor_t *cursor;\n    bson_t *pipeline;\n    bson_t *search_stage;\n    bson_t *project_stage;\n    const bson_t *doc;\n    bson_error_t error;\n\n    // Connect to MongoDB\n    client = connect_to_mongodb();\n    if (!client) {\n        return EXIT_FAILURE;\n    }\n\n    printf(\"Connected to MongoDB Atlas\\n\");\n\n    // Get database and collection\n    database = mongoc_client_get_database(client, DATABASE_NAME);\n    collection = mongoc_client_get_collection(client, DATABASE_NAME, COLLECTION_NAME);\n\n    // Create aggregation pipeline\n    pipeline = bson_new();\n    \n    // Create $search stage\n    search_stage = bson_new();\n    bson_t *search_spec = bson_new();\n    bson_t *wildcard_spec = bson_new();\n    \n    BSON_APPEND_UTF8(wildcard_spec, \"path\", \"title\");\n    BSON_APPEND_UTF8(wildcard_spec, \"query\", \"how*\");\n    BSON_APPEND_BOOL(wildcard_spec, \"allowAnalyzedField\", true);\n    \n    BSON_APPEND_UTF8(search_spec, \"index\", \"partial-match-tutorial\");\n    BSON_APPEND_DOCUMENT(search_spec, \"wildcard\", wildcard_spec);\n    \n    BSON_APPEND_DOCUMENT(search_stage, \"$search\", search_spec);\n    \n    // Create $project stage\n    project_stage = bson_new();\n    bson_t *project_spec = bson_new();\n    BSON_APPEND_INT32(project_spec, \"_id\", 0);\n    BSON_APPEND_INT32(project_spec, \"title\", 1);\n    BSON_APPEND_DOCUMENT(project_stage, \"$project\", project_spec);\n    \n    // Build the pipeline array\n    bson_t *stage_array = bson_new();\n    BSON_APPEND_DOCUMENT(stage_array, \"0\", search_stage);\n    BSON_APPEND_DOCUMENT(stage_array, \"1\", project_stage);\n    \n    BSON_APPEND_ARRAY(pipeline, \"pipeline\", stage_array);\n\n    printf(\"Running wildcard search query...\\n\");\n\n    // Execute the aggregation\n    cursor = mongoc_collection_aggregate(collection, MONGOC_QUERY_NONE, pipeline, NULL, NULL);\n    \n    if (!cursor) {\n        fprintf(stderr, \"Failed to execute aggregation\\n\");\n        goto cleanup;\n    }\n\n    printf(\"Results:\\n\");\n    \n    // Iterate through results\n    while (mongoc_cursor_next(cursor, &doc)) {\n        print_bson_document(doc);\n    }\n\n    // Check for errors\n    if (mongoc_cursor_error(cursor, &error)) {\n        fprintf(stderr, \"Cursor error: %s\\n\", error.message);\n    }\n\ncleanup:\n    // Cleanup\n    if (cursor) {\n        mongoc_cursor_destroy(cursor);\n    }\n    bson_destroy(pipeline);\n    bson_destroy(search_stage);\n    bson_destroy(project_stage);\n    bson_destroy(search_spec);\n    bson_destroy(wildcard_spec);\n    bson_destroy(project_spec);\n    bson_destroy(stage_array);\n    mongoc_collection_destroy(collection);\n    mongoc_database_destroy(database);\n    cleanup_mongodb(client);\n\n    printf(\"Wildcard query completed\\n\");\n    return EXIT_SUCCESS;\n}\n",
      "tokenEstimate": 966
    },
    {
      "language": "bash",
      "content": "gcc -o partial-match-tutorial-query partial-match-tutorial-query.c $(pkg-config --cflags --libs libmongoc-1.0)\n./partial-match-tutorial-query",
      "tokenEstimate": 36
    }
  ],
  "slug": "run-a-case-sensitive-partial-match-query--local-onprem-driver-c",
  "selections": {
    "deployment-type": "local-onprem",
    "interface": "driver",
    "language": "c"
  },
  "variantAgnostic": false
}
