{
  "heading": "Atlas Resource Policy Examples",
  "headingLevel": 2,
  "body": "Copy and modify the following example policies to use for your organization.\nAll examples use [Cedar policy language](https://docs.cedarpolicy.com/), but for the Atlas Administration API, the examples are wrapped\nin JSON (Javascript Object Notation) so you can paste them directly into your API (Application Programming Interface) calls.\nTo learn more, see [Create an Atlas Resource Policy Configuration](procedure-create-atlas-resource-policy).\n\n**Api:**\n\nAtlas Administration API\n\nThe following example prevents users from creating more than `2`\nclusters in a project:\n\n```\n{\n   \"name\": \"Prohibit More Than 2 Clusters in a Project\",\n   \"policies\": [\n      {\n        \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.project.clustersInProject > 2 };\"\n      }\n   ]\n}\n```\n\nThe following example prevents users from creating a cluster\non Google Cloud:\n\n```\n{\n   \"name\": \"Policy Preventing GCP Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.cloudProviders.contains(ResourcePolicy::CloudProvider::\\\"gcp\\\") };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `unless` clause to allow users to\ncreate clusters *only* on Google Cloud:\n\n```\n{\n   \"name\": \"Policy Allowing Only GCP Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"] };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `when` clause to prevent users from\ncreating or editing a cluster in the project with ID `6217f7fff7957854e2d09179`\nunless Google Cloud is the only cloud provider:\n\n```\n{\n   \"name\": \"Policy Allowing Only GCP Clusters for One Project\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { resource in ResourcePolicy::Project::\\\"6217f7fff7957854e2d09179\\\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"] };\"\n      }\n   ]\n}\n```\n\nThe following example prevents modifications to the cluster with ID\n`3217e2gdf79a4c54e2d0827` when Google Cloud is the cloud provider:\n\n```\n{\n   \"name\": \"Forbid Modifications to Specific GCP Cluster\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { resource in ResourcePolicy::Project::\\\"65dcbf5ccd12a54df59a54e6\\\" && resource == ResourcePolicy::Cluster::\\\"670968dfc0a2297ef46bc02a\\\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"]};\"\n      }\n   ]\n}\n```\n\nThe following example prevents users from creating or editing a cluster\nin the region `aws:us-east-1`:\n\n```\n{\n   \"name\": \"Prohibit Cluster Deployment in the US-EAST-1 Region\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal,action == ResourcePolicy::Action::\\\"cluster.modify\\\",resource) when {context.cluster.regions.contains(ResourcePolicy::Region::\\\"aws:us-east-1\\\")};\"\n      }\n   ]\n}\n```\n\nThe following example prevents users from creating a cluster\nin the region `aws:us-west-1`:\n\n```\n{\n   \"name\": \"Policy Preventing Clusters in AWS:us-west-1\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.regions.contains(ResourcePolicy::Region::\\\"aws:us-west-1\\\") };\"\n      }\n   ]\n}\n```\n\nThe following example prevents users from creating a cluster\nin the regions `aws:us-east-1`, `aws:us-west-1`, or `azure:westeurope`:\n\n```\n{\n   \"name\": \"Policy Preventing Clusters in 3 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"aws:us-west-1\\\", ResourcePolicy::Region::\\\"azure:westeurope\\\"]) };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `unless` clause to allow users to\ncreate clusters *only* in the regions `aws:us-east-1` and `azure:westeurope`:\n\n```\n{\n   \"name\": \"Policy Allowing Clusters Only in 2 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { [ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"azure:westeurope\\\"].containsAll(context.cluster.regions) };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `when` clause to restrict users from\nediting the cluster with ID `3217e2gdf79a4c54e2d0827`\nin the regions `aws:us-east-1` and `aws:us-west-1`:\n\n```\n{\n   \"name\": \"Policy Restricting Edits to One Cluster from 2 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource == ResourcePolicy::Cluster::\\\"3217e2gdf79a4c54e2d0827\\\") when { context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\",ResourcePolicy::Region::\\\"aws:us-west-1\\\"]) };\"\n      }\n   ]\n}\n```\n\nThe following example restricts Atlas users from creating Atlas clusters\non Google Cloud, or in the AWS (Amazon Web Services) regions `aws:us-east-1` or `aws:us-west-1`.\n\n```\n{\n   \"name\": \"Policy Restricting All GCP Clusters and Some AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal,action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when {context.cluster.cloudProviders.containsAny([ResourcePolicy::CloudProvider::\\\"gcp\\\"]) || context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"aws:us-west-1\\\"])};\"}\n      }\n   ]\n}\n```\n\n> **Note:** If you add the `0.0.0.0/0` CIDR to a project access list,\n> Atlas sends an alert email to all users directly\n> granted a role to the project, or indirectly granted a role through\n> team membership (if the team is granted a role to the\n> project).\n\nThe following example prevents users from editing a project\nfrom a wildcard IP (`0.0.0.0/0`):\n\n```\n{\n   \"name\": \"Policy Restricting Wildcard IP\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) when { context.project.ipAccessList.contains(ip(\\\"0.0.0.0/0\\\")) };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `unless` clause to allow users to\nedit projects *only* from the IP addresses `1.2.3.4/32`, `8.8.8.8/32`,\nand `4.4.4.4/32`:\n\n```\n{\n   \"name\": \"Policy Restricting Project Edits to Specified IPs\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) unless { [ip(\\\"1.2.3.4/32\\\"), ip(\\\"8.8.8.8/32\\\"), ip(\\\"4.4.4.4/32\\\")].containsAll(context.project.ipAccessList) };\"\n      }\n   ]\n}\n```\n\nThe following example ensures that all traffic to the cluster is prohibited\nover public networks by requiring the IP access list to be empty.\n\n```\n{\n   \"name\": \"Policy Preventing Access Over Public Networks\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) unless {context.project.ipAccessList.isEmpty() };\"\n      }\n   ]\n}\n```\n\nThe following example uses the `when` clause to restrict Atlas from\nprovisioning or scaling  clusters to less than `M30` or greater than `M60`:\n\n> **Note:** This policy doesn't restrict clusters with a [class](storage-class-ui) of  **Low CPU** or **NVMe SSD**.\n\n```\n{\n   \"name\": \"Policy Restricting Min/Max Cluster Size\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { (context.cluster has minGeneralClassInstanceSizeValue && context.cluster.minGeneralClassInstanceSizeValue < 30) || (context.cluster has maxGeneralClassInstanceSizeValue && context.cluster.maxGeneralClassInstanceSizeValue > 60) };\"\n      }\n   ]\n}\n```\n\nThe following example requires that a project has a [Configure Maintenance Window](configure-maintenance-window) configured:\n\n```\n{\n   \"name\": \"Policy Enforcing Existence of a Project Maintenance Window\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.maintenanceWindow.modify\\\", resource) when {context.project.hasDefinedMaintenanceWindow == false};\"\n      }\n   ]\n}\n```\n\nThe following example prevents modifications to VPC (Virtual Private Cloud) peering connections across\ndifferent cloud providers (AWS (Amazon Web Services), Google Cloud, Azure (Microsoft Azure)).\n\nEach cloud provider requires different details for VPC (Virtual Private Cloud) peering. Gather the following\ndetails for your cloud provider and replace them in the example:\n\n**AWS:** `aws:<AWS_ACCOUNT_ID>:<VPC_ID>:<VPC_CIDR>`\n\n- `AWS_ACCOUNT_ID`: Your AWS (Amazon Web Services) account number.\n\n- `VPC_ID`: The ID of the VPC (Virtual Private Cloud).\n\n- `VPC_CIDR`: The CIDR (Classless Inter-Domain Routing) block of the VPC (Virtual Private Cloud).\n\n**Azure:** `azure:<SUBSCRIPTION_ID>:<RESOURCE_GROUP_NAME>:<VNET_NAME>`\n\n- `SUBSCRIPTION_ID`: Your Azure (Microsoft Azure) subscription ID.\n\n- `RESOURCE_GROUP_NAME`: The resource group in which your Virtual Network (VNet) exists.\n\n- `VNET_NAME`: The name of your VNet.\n\n**Google Cloud:** `gcp:<GCP_PROJECT_ID>:<VPC_NAME>`\n\n- `GCP_PROJECT_ID`: The ID of your Google Cloud project.\n\n- `VPC_NAME`: The name of the VPC (Virtual Private Cloud) in Google Cloud.\n\n```\n{\n   \"name\": \"Policy Preventing Modifications to Peering Across Providers\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.vpcPeering.modify\\\", resource) when {context.project.peeringConnections == [\\\"aws:000123456789:vpc-0316c47cc923ce313:10.0.0.0/16\\\", \\\"azure:fd01aafc-b3re-2193-8497-83lp3m83a1a5:rg-name:vnet\\\", \\\"gcp:inductive-cocoa-108200:default\\\"]};\"\n      }\n   ]\n}\n```\n\nThe following example prevents modifications to private endpoint connections across\ncloud providers (AWS (Amazon Web Services), Google Cloud, Azure (Microsoft Azure)).\n\nEach cloud provider requires different details for private endpoints. Gather the\nfollowing details for your cloud provider and replace them in the example:\n\n**AWS:** `aws:<VPC_ENDPOINT_ID>`\n\n- `VPC_ENDPOINT_ID`: The ID of the AWS (Amazon Web Services) VPC (Virtual Private Cloud) endpoint.\n\n**Azure:** `azure:<PRIVATE_ENDPOINT_RESOURCE_ID>:<PRIVATE_ENDPOINT_IP_ADDRESS>`\n\n- `PRIVATE_ENDPOINT_RESOURCE_ID`: The full resource ID path of the Azure (Microsoft Azure) private endpoint.\n\n- `PRIVATE_ENDPOINT_IP_ADDRESS`: The IP address assigned to the private endpoint.\n\n**Google Cloud:** `gcp:<GCP_PROJECT_ID>:<VPC_NAME>`\n\n- `GCP_PROJECT_ID`: The ID of your Google Cloud project.\n\n- `VPC_NAME`: The name of the VPC (Virtual Private Cloud) in Google Cloud associated with the connection.\n\n```\n{\n   \"name\": \"Policy Preventing Modifications to Private Endpoints Across Providers\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.privateEndpoint.modify\\\", resource) when {context.project.privateEndpoints == [\\\"aws:vpce-042d72ded1748f314\\\", \\\"azure:/subscriptions/fd01aafc-b3re-2193-8497-83lp3m83a1a5/resourceGroups/rg-name/providers/Microsoft.Network/privateEndpoints/pe-name:10.0.0.4\\\", \\\"gcp:inductive-cocoa-108200:default\\\"]};\"\n      }\n   ]\n}\n```\n\nThe following example restricts the minimum TLS (Transport Layer Security) version that your cluster\naccepts for incoming connections to TLS (Transport Layer Security) 1.2.\n\nPossible values for `minTLSVersion` include:\n\n- **TLS 1.0**: `ResourcePolicy::TLSVersion::\\\"tls1_0\\\"`\n\n- **TLS 1.1**: `ResourcePolicy::TLSVersion::\\\"tls1_1\\\"`\n\n- **TLS 1.2**: `ResourcePolicy::TLSVersion::\\\"tls1_2\\\"`\n\n```\n{\n   \"name\": \"Policy Restricting Cluster Connections to Minimum TLS 1.2\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.minTLSVersion == ResourcePolicy::TLSVersion::\\\"tls1_2\\\"};\"\n      }\n   ]\n}\n```\n\nThe following example requires that clusters use the custom TLS (Transport Layer Security) cipher suite configuration\n`TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384` by setting `ResourcePolicy::CipherConfigMode::\\\"custom\\\"`.\n\nPossible values for custom TLS (Transport Layer Security) cipher suite configurations are:\n\n- `ResourcePolicy::CipherSuite::\\\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\\\"`\n\n- `ResourcePolicy::CipherSuite::\\\"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256\\\"`\n\n```\n{\n   \"name\": \"Policy Requiring Specific TLS Cipher Suite\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\\\"custom\\\" && context.cluster.cipherSuites == [ResourcePolicy::CipherSuite::\\\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\\\"]};\"\n      }\n   ]\n}\n```\n\nThe following example requires that clusters use the default TLS (Transport Layer Security) cipher suite configuration.\n\n```\n{\n   \"name\": \"Policy Requiring Default TLS Cipher Suite\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\\\"default\\\"};\"\n      }\n   ]\n}\n```\n\nThe following example enforces a maximum disk size of 4 TB by forbidding any\nvalue greater than (`>`) the limit.\n\nTo prevent creating clusters smaller than a certain size,\nuse a less-than operator (`<`) in your forbid policy.\n\n```\n{\n   \"name\": \"Limit Max Disk Size to 4000GB\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096 };\"\n      }\n   ]\n}\n```\n\nThe following example enables users to create *only* replica set clusters.\n\n```\n{\n   \"name\": \"Require Replica Set Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { context.cluster.clusterType == ResourcePolicy::ClusterType::\\\"replicaset\\\" };\"\n      }\n   ]\n}\n```\n\nTo allow a different cluster topology, use one of the following values:\n\n- **Sharded Cluster**: `ResourcePolicy::ClusterType::\"sharded\"` Used for standard\n  horizontal scaling, where data is distributed across multiple shards to support\n  large datasets or high throughput.\n\n- **Global Cluster**: `ResourcePolicy::ClusterType::\"geosharded\"` Used for\n  location-aware deployments, where data is distributed across specific geographic\n  zones to ensure data resides close to users.\n\nThe following example requires that sharded clusters have at least three shards.\n\n> **Important:**&#x20;\n>\n> - If you enforce a minimum shard count greater than one,\n>   you must explicitly allow the transition state for cluster conversions in your policy\n>   (for example, `&& !context.cluster.isConvertingToSharded`).\n>   Without this exception, Atlas blocks users from converting a replica set to\n>   a sharded cluster because the conversion process requires an intermediate state\n>   where the cluster has only one shard.\n>\n> - Atlas treats a replica set as having a shard count of one. If you enforce a\n>   minimum shard count greater than one without checking whether clusters are sharded clusters, all replica sets in the project are marked as non-compliant.\n>\n> - Use `minShardCount` to enforce *minimums* only (for example, `< 3`) and\n>   `maxShardCount` to enforce *maximums* only (for example, `> 10`).\n>   Using opposite comparison operators with these properties causes policy evaluation errors.\n\n```\n{\n   \"name\": \"Require Minimum 3 Shards\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.minShardCount < 3 && !context.cluster.isConvertingToSharded };\"\n      }\n   ]\n}\n```\n\nThe following example prevents users from creating sharded clusters\nwith [About Config Servers Deployment](nodes-for-config-server), ensuring they use dedicated config servers from\nthe start.\n\n```\n{\n   \"name\": \"Enforce config server management mode to fixed_to_dedicated for sharded clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.clusterType == ResourcePolicy::ClusterType::\\\"sharded\\\" && context.cluster has configServerManagementMode && context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\\\"fixed_to_dedicated\\\" };\"\n      }\n   ]\n}\n```\n\n**Ui:**\n\nAtlas UI\n\nThe following example prevents users from creating more than `2`\nclusters in a project:\n\n```\nforbid (\n    principal,\n    action == ResourcePolicy::Action::\"cluster.modify\",\n    resource\n)\nwhen { context.project.clustersInProject > 2 };\n```\n\nThe following example prevents users from creating a cluster\non Google Cloud:\n\n```\nforbid (\n    principal,\n    action == ResourcePolicy::Action::\"cluster.modify\",\n    resource\n)\nwhen { context.cluster.cloudProviders.contains(ResourcePolicy::CloudProvider::\"gcp\") };\n```\n\nThe following example uses the `unless` clause to allow users to\ncreate clusters *only* on Google Cloud:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] };\n```\n\nThe following example uses the `when` clause to prevent users from\ncreating or editing a cluster in the project with ID `6217f7fff7957854e2d09179`\nunless Google Cloud is the only cloud provider:\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {resource in ResourcePolicy::Project::\"6217f7fff7957854e2d09179\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] };\n```\n\nThe following example prevents modifications to the cluster with ID\n`3217e2gdf79a4c54e2d0827` when Google Cloud is the cloud provider:\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {resource in ResourcePolicy::Project::\"65dcbf5ccd12a54df59a54e6\" && resource == ResourcePolicy::Cluster::\"670968dfc0a2297ef46bc02a\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"]};\n```\n\nThe following example prevents users from creating or editing a cluster\nin the region `aws:us-east-1`:\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {context.cluster.regions.contains(ResourcePolicy::Region::\"aws:us-east-1\")};\n```\n\nThe following example prevents users from creating a cluster\nin the region `aws:us-west-1`:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { context.cluster.regions.contains(ResourcePolicy::Region::\"aws:us-west-1\") };\n```\n\nThe following example prevents users from creating a cluster\nin the regions `aws:us-east-1`, `aws:us-west-1`, or `azure:westeurope`:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { context.cluster.regions.containsAny([ResourcePolicy::Region::\"aws:us-east-1\",ResourcePolicy::Region::\"aws:us-west-1\",ResourcePolicy::Region::\"azure:westeurope\"]) };\n```\n\nThe following example uses the `unless` clause to allow users to\ncreate clusters *only* in the regions `aws:us-east-1` and `azure:westeurope`:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\\\"cluster.modify\\\",\n   resource\n )\n unless { [ResourcePolicy::Region::\"aws:us-east-1\", ResourcePolicy::Region::\"azure:westeurope\"].containsAll(context.cluster.regions) };\n```\n\nThe following example uses the `when` clause to restrict users from\nediting the cluster with ID `3217e2gdf79a4c54e2d0827`\nin the regions `aws:us-east-1` and `aws:us-west-1`:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource == ResourcePolicy::Cluster::\"3217e2gdf79a4c54e2d0827\"\n )\n when { context.cluster.regions.containsAny([ResourcePolicy::Region::\"aws:us-east-1\",ResourcePolicy::Region::\"aws:us-west-1\"]) };\n```\n\n> **Note:** If you add the `0.0.0.0/0` CIDR to a project access list,\n> Atlas sends an alert email to all users directly\n> granted a role to the project, or indirectly granted a role through\n> team membership (if the team is granted a role to the\n> project).\n\nThe following example prevents users from editing a project\nfrom a wildcard IP (`0.0.0.0/0`):\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n when { context.project.ipAccessList.contains(ip(\"0.0.0.0/0\")) };\n```\n\nThe following example uses the `unless` clause to allow users to\nedit projects *only* from the IP addresses `1.2.3.4/32`, `8.8.8.8/32`,\nand `4.4.4.4/32`:\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n unless { [ip(\"1.2.3.4/32\"), ip(\"8.8.8.8/32\"), ip(\"4.4.4.4/32\")].containsAll(context.project.ipAccessList) };\n```\n\nThe following example ensures that all traffic to the cluster is prohibited\nover public networks by requiring the IP access list to be empty.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n unless { context.project.ipAccessList.isEmpty() };\n```\n\nThe following example uses the `when` clause to restrict Atlas from\nprovisioning or scaling  clusters to less than `M30` or greater than `M60`:\n\n> **Note:** This policy doesn't restrict clusters with a [class](storage-class-ui) of  **Low CPU** or **NVMe SSD**.\n\n```\n forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { (context.cluster has minGeneralClassInstanceSizeValue && context.cluster.minGeneralClassInstanceSizeValue < 30) || (context.cluster has maxGeneralClassInstanceSizeValue && context.cluster.maxGeneralClassInstanceSizeValue > 60) };\n```\n\nThe following example requires that a project has a [Configure Maintenance Window](configure-maintenance-window) configured:\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.maintenanceWindow.modify\",\n   resource\n )\n when {context.project.hasDefinedMaintenanceWindow == false};\n```\n\nThe following example prevents modifications to VPC (Virtual Private Cloud) peering connections across\ndifferent cloud providers (AWS (Amazon Web Services), Google Cloud, Azure (Microsoft Azure)).\n\nEach cloud provider requires different details for VPC (Virtual Private Cloud) peering. Gather the following\ndetails for your cloud provider and replace them in the example:\n\n**AWS:** `aws:<AWS_ACCOUNT_ID>:<VPC_ID>:<VPC_CIDR>`\n\n- `AWS_ACCOUNT_ID`: Your AWS (Amazon Web Services) account number.\n\n- `VPC_ID`: The ID of the VPC (Virtual Private Cloud).\n\n- `VPC_CIDR`: The CIDR (Classless Inter-Domain Routing) block of the VPC (Virtual Private Cloud).\n\n**Azure:** `azure:<SUBSCRIPTION_ID>:<RESOURCE_GROUP_NAME>:<VNET_NAME>`\n\n- `SUBSCRIPTION_ID`: Your Azure (Microsoft Azure) subscription ID.\n\n- `RESOURCE_GROUP_NAME`: The resource group in which your Virtual Network (VNet) exists.\n\n- `VNET_NAME`: The name of your VNet.\n\n**Google Cloud:** `gcp:<GCP_PROJECT_ID>:<VPC_NAME>`\n\n- `GCP_PROJECT_ID`: The ID of your Google Cloud project.\n\n- `VPC_NAME`: The name of the VPC (Virtual Private Cloud) in Google Cloud.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.vpcPeering.modify\",\n   resource\n )\n when {context.project.peeringConnections == [\"aws:000123456789:vpc-0316c47cc923ce313:10.0.0.0/16\", \"azure:fd01aafc-b3re-2193-8497-83lp3m83a1a5:rg-name:vnet\", \"gcp:inductive-cocoa-108200:default\"]};\n```\n\nThe following example prevents modifications to private endpoint connections across\ncloud providers (AWS (Amazon Web Services), Google Cloud, Azure (Microsoft Azure)).\n\nEach cloud provider requires different details for private endpoints. Gather the\nfollowing details for your cloud provider and replace them in the example:\n\n**AWS:** `aws:<VPC_ENDPOINT_ID>`\n\n- `VPC_ENDPOINT_ID`: The ID of the AWS (Amazon Web Services) VPC (Virtual Private Cloud) endpoint.\n\n**Azure:** `azure:<PRIVATE_ENDPOINT_RESOURCE_ID>:<PRIVATE_ENDPOINT_IP_ADDRESS>`\n\n- `PRIVATE_ENDPOINT_RESOURCE_ID`: The full resource ID path of the Azure (Microsoft Azure) private endpoint.\n\n- `PRIVATE_ENDPOINT_IP_ADDRESS`: The IP address assigned to the private endpoint.\n\n**Google Cloud:** `gcp:<GCP_PROJECT_ID>:<PRIVATE_ENDPOINT_GROUP_NAME>`\n\n- `GCP_PROJECT_ID`: The ID of your Google Cloud project.\n\n- `PRIVATE_ENDPOINT_GROUP_NAME`: The name of the private endpoint group in Google Cloud associated with the connection.\n\n**Google Cloud Port-based Routing:** `gcp:<GCP_PROJECT_ID>:<PRIVATE_ENDPOINT_NAME>:<PRIVATE_ENDPOINT_IP_ADDRESS>`\n\n- `GCP_PROJECT_ID`: The ID of your Google Cloud project.\n\n- `PRIVATE_ENDPOINT_NAME`: The name of the private endpoint in Google Cloud associated with the connection.\n\n- `PRIVATE_ENDPOINT_IP_ADDRESS`: The IP address assigned to the private endpoint.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.privateEndpoint.modify\",\n   resource\n )\n when {context.project.privateEndpoints == [\"aws:vpce-042d72ded1748f314\", \"azure:/subscriptions/fd01aafc-b3re-2193-8497-83lp3m83a1a5/resourceGroups/rg-name/providers/Microsoft.Network/privateEndpoints/pe-name:10.0.0.4\", \"gcp:inductive-cocoa-108200:default\"]};\n```\n\nThe following example restricts the minimum TLS (Transport Layer Security) version that your cluster\naccepts for incoming connections to TLS (Transport Layer Security) 1.2.\n\nPossible values for `minTLSVersion` include:\n\n- **TLS 1.0**: `ResourcePolicy::TLSVersion::\"tls1_0\"`\n\n- **TLS 1.1**: `ResourcePolicy::TLSVersion::\"tls1_1\"`\n\n- **TLS 1.2**: `ResourcePolicy::TLSVersion::\"tls1_2\"`\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n { context.cluster.minTLSVersion == ResourcePolicy::TLSVersion::\"tls1_2\" };\n```\n\nThe following example requires that clusters use the custom TLS (Transport Layer Security) cipher suite configuration\n`TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384` by setting `ResourcePolicy::CipherConfigMode::\"custom\"`.\n\nPossible values for custom TLS (Transport Layer Security) cipher suite configurations are:\n\n- `ResourcePolicy::CipherSuite::\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\"`\n\n- `ResourcePolicy::CipherSuite::\"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256\"`\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\"custom\" &&\n   context.cluster.cipherSuites == [ResourcePolicy::CipherSuite::\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\"]\n };\n```\n\nThe following example requires that clusters use the default TLS (Transport Layer Security) cipher suite configuration.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\"default\"\n };\n```\n\nThe following example enforces a maximum disk size of 4 TB by forbidding any\nvalue greater than (`>`) the limit.\n\nTo prevent creating clusters smaller than a certain size,\nuse a less-than operator (`<`) in your forbid policy.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096\n };\n```\n\nThe following example enables users to create *only* replica set clusters.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.clusterType == ResourcePolicy::ClusterType::\"replicaset\"\n };\n```\n\nTo allow a different cluster topology, use one of the following values:\n\n- **Sharded Cluster**: `ResourcePolicy::ClusterType::\"sharded\"` Used for standard\n  horizontal scaling, where data is distributed across multiple shards to support\n  large datasets or high throughput.\n\n- **Global Cluster**: `ResourcePolicy::ClusterType::\"geosharded\"` Used for\n  location-aware deployments, where data is distributed across specific geographic\n  zones to ensure data resides close to users.\n\nThe following example requires that sharded clusters have at least three shards.\n\n> **Important:**&#x20;\n>\n> - If you enforce a minimum shard count greater than one,\n>   you must explicitly allow the transition state for cluster conversions in your policy\n>   (for example, `&& !context.cluster.isConvertingToSharded`).\n>   Without this exception, Atlas blocks users from converting a replica set to\n>   a sharded cluster because the conversion process requires an intermediate state\n>   where the cluster has only one shard.\n>\n> - Atlas treats a replica set as having a shard count of one. If you enforce a\n>   minimum shard count greater than one without checking whether clusters are sharded clusters, all replica sets in the project are marked as non-compliant.\n>\n> - Use `minShardCount` to enforce *minimums* only (for example, `< 3`) and\n>   `maxShardCount` to enforce *maximums* only (for example, `> 10`).\n>   Using opposite comparison operators with these properties causes policy evaluation errors.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster.minShardCount < 3 && !context.cluster.isConvertingToSharded\n };\n```\n\nThe following example prevents users from creating sharded clusters\nwith [About Config Servers Deployment](nodes-for-config-server), ensuring they use dedicated config servers from\nthe start.\n\n```\n forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster.clusterType == ResourcePolicy::ClusterType::\"sharded\" &&\n   context.cluster has configServerManagementMode &&\n   context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\"fixed_to_dedicated\"\n };\n```\n\n**Tf:**\n\nTerraform\n\nThe following example prevents users from creating more than `2`\nclusters in a project:\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"forbid_more_than_two_clusters\" {\n  org_id = var.org_id\n  name   = \"forbid-more-than-two-clusters\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when { context.project.clustersInProject > 2 };\n      EOF\n    },\n  ]\n}\n```\n\nThe following example prevents users from creating a cluster\non all cloud providers except AWS (Amazon Web Services):\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"allow_only_aws_cloud_provider\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-aws-cloud-provider\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        unless\n          { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"aws\"] };\n      EOF\n  }]\n}\n```\n\nThe following example uses the `unless` clause to allow users to\ncreate clusters *only* in the regions `aws:us-east-1` and\n`aws:eu-central-1`:\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"allow_only_regions\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-regions\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n          principal,\n          action == ResourcePolicy::Action::\"cluster.modify\",\n          resource\n        ) unless {\n            [ResourcePolicy::Region::\"aws:eu-west-1\", ResourcePolicy::Region::\"aws:eu-central-1\"]\n              .containsAll(context.cluster.regions)};\n      EOF\n    },\n  ]\n}\n```\n\nThe following example uses the `unless` clause to allow users to create\nclusters *only* on Google Cloud or in the regions `aws:us-east-1` and\n`aws:eu-central-1`:\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"allow_only_provider_regions\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-provider-regions\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n          principal,\n          action == ResourcePolicy::Action::\"cluster.modify\",\n          resource\n        ) unless {\n            context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] ||\n            [ResourcePolicy::Region::\"aws:eu-west-1\", ResourcePolicy::Region::\"aws:eu-central-1\"]\n              .containsAll(context.cluster.regions)};\n      EOF\n    },\n  ]\n}\n```\n\n> **Note:** If you add the `0.0.0.0/0` CIDR to a project access list,\n> Atlas sends an alert email to all users directly\n> granted a role to the project, or indirectly granted a role through\n> team membership (if the team is granted a role to the\n> project).\n\nThe following example prevents users from editing a project\nfrom a wildcard IP (`0.0.0.0/0`):\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"forbid_project_access_anywhere\" {\n  org_id = var.org_id\n  name   = \"forbid-project-access-anywhere\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n            resource\n        )\n        when {context.project.ipAccessList.contains(ip(\"0.0.0.0/0\"))};\n    EOF\n    },\n  ]\n}\n```\n\nThe following example enforces a maximum disk size of 4 TB by forbidding any\nvalue greater than (`>`) the limit.\n\nTo prevent creating clusters smaller than a certain size,\nuse a less-than operator (`<`) in your forbid policy.\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"limit_max_disk_size\" {\n  org_id = var.org_id\n  name   = \"limit-max-4096gb-disk\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when { context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096 };\n      EOF\n    },\n  ]\n}\n```\n\nThe following example enables users to create *only* replica set clusters.\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"require_replica_set\" {\n  org_id = var.org_id\n  name   = \"require-replica-set\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        unless { context.cluster.clusterType == ResourcePolicy::ClusterType::\"replicaset\" };\n      EOF\n    },\n  ]\n}\n```\n\nTo allow a different cluster topology, use one of the following values:\n\n- **Sharded Cluster**: `ResourcePolicy::ClusterType::\"sharded\"` Used for standard\n  horizontal scaling, where data is distributed across multiple shards to support\n  large datasets or high throughput.\n\n- **Global Cluster**: `ResourcePolicy::ClusterType::\"geosharded\"` Used for\n  location-aware deployments, where data is distributed across specific geographic\n  zones to ensure data resides close to users.\n\nThe following example requires that sharded clusters have at least three shards.\n\n> **Important:**&#x20;\n>\n> - If you enforce a minimum shard count greater than one,\n>   you must explicitly allow the transition state for cluster conversions in your policy\n>   (for example, `&& !context.cluster.isConvertingToSharded`).\n>   Without this exception, Atlas blocks users from converting a replica set to\n>   a sharded cluster because the conversion process requires an intermediate state\n>   where the cluster has only one shard.\n>\n> - Atlas treats a replica set as having a shard count of one. If you enforce a\n>   minimum shard count greater than one without checking whether clusters are sharded clusters, all replica sets in the project are marked as non-compliant.\n>\n> - Use `minShardCount` to enforce *minimums* only (for example, `< 3`) and\n>   `maxShardCount` to enforce *maximums* only (for example, `> 10`).\n>   Using opposite comparison operators with these properties causes policy evaluation errors.\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"require_min_shards\" {\n  org_id = var.org_id\n  name   = \"require-min-3-shards\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when {\n          context.cluster.minShardCount < 3 &&\n          !context.cluster.isConvertingToSharded\n        };\n      EOF\n    },\n  ]\n}\n```\n\nThe following example prevents users from creating sharded clusters\nwith [About Config Servers Deployment](nodes-for-config-server), ensuring they use dedicated config servers from\nthe start.\n\n```terraform\nresource \"mongodbatlas_resource_policy\" \"enforce_dedicated_config_server\" {\n  org_id = var.org_id\n  name   = \"enforce-dedicated-config-server\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when {\n          context.cluster.clusterType == ResourcePolicy::ClusterType::\"sharded\" &&\n          context.cluster has configServerManagementMode &&\n          context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\"fixed_to_dedicated\"\n        };\n      EOF\n    },\n  ]\n}\n```",
  "codeBlocks": [
    {
      "language": "text",
      "content": "{\n   \"name\": \"Prohibit More Than 2 Clusters in a Project\",\n   \"policies\": [\n      {\n        \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.project.clustersInProject > 2 };\"\n      }\n   ]\n}",
      "tokenEstimate": 67
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing GCP Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.cloudProviders.contains(ResourcePolicy::CloudProvider::\\\"gcp\\\") };\"\n      }\n   ]\n}",
      "tokenEstimate": 69
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Allowing Only GCP Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"] };\"\n      }\n   ]\n}",
      "tokenEstimate": 71
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Allowing Only GCP Clusters for One Project\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { resource in ResourcePolicy::Project::\\\"6217f7fff7957854e2d09179\\\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"] };\"\n      }\n   ]\n}",
      "tokenEstimate": 97
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Forbid Modifications to Specific GCP Cluster\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { resource in ResourcePolicy::Project::\\\"65dcbf5ccd12a54df59a54e6\\\" && resource == ResourcePolicy::Cluster::\\\"670968dfc0a2297ef46bc02a\\\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\\\"gcp\\\"]};\"\n      }\n   ]\n}",
      "tokenEstimate": 119
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Prohibit Cluster Deployment in the US-EAST-1 Region\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal,action == ResourcePolicy::Action::\\\"cluster.modify\\\",resource) when {context.cluster.regions.contains(ResourcePolicy::Region::\\\"aws:us-east-1\\\")};\"\n      }\n   ]\n}",
      "tokenEstimate": 77
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing Clusters in AWS:us-west-1\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.regions.contains(ResourcePolicy::Region::\\\"aws:us-west-1\\\") };\"\n      }\n   ]\n}",
      "tokenEstimate": 77
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing Clusters in 3 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"aws:us-west-1\\\", ResourcePolicy::Region::\\\"azure:westeurope\\\"]) };\"\n      }\n   ]\n}",
      "tokenEstimate": 103
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Allowing Clusters Only in 2 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { [ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"azure:westeurope\\\"].containsAll(context.cluster.regions) };\"\n      }\n   ]\n}",
      "tokenEstimate": 92
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting Edits to One Cluster from 2 AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource == ResourcePolicy::Cluster::\\\"3217e2gdf79a4c54e2d0827\\\") when { context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\",ResourcePolicy::Region::\\\"aws:us-west-1\\\"]) };\"\n      }\n   ]\n}",
      "tokenEstimate": 118
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting All GCP Clusters and Some AWS Regions\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal,action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when {context.cluster.cloudProviders.containsAny([ResourcePolicy::CloudProvider::\\\"gcp\\\"]) || context.cluster.regions.containsAny([ResourcePolicy::Region::\\\"aws:us-east-1\\\", ResourcePolicy::Region::\\\"aws:us-west-1\\\"])};\"}\n      }\n   ]\n}",
      "tokenEstimate": 113
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting Wildcard IP\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) when { context.project.ipAccessList.contains(ip(\\\"0.0.0.0/0\\\")) };\"\n      }\n   ]\n}",
      "tokenEstimate": 76
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting Project Edits to Specified IPs\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) unless { [ip(\\\"1.2.3.4/32\\\"), ip(\\\"8.8.8.8/32\\\"), ip(\\\"4.4.4.4/32\\\")].containsAll(context.project.ipAccessList) };\"\n      }\n   ]\n}",
      "tokenEstimate": 109
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing Access Over Public Networks\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.ipAccessList.modify\\\", resource) unless {context.project.ipAccessList.isEmpty() };\"\n      }\n   ]\n}",
      "tokenEstimate": 64
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting Min/Max Cluster Size\",\n   \"policies\": [\n      {\n         \"body\": \"forbid(principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { (context.cluster has minGeneralClassInstanceSizeValue && context.cluster.minGeneralClassInstanceSizeValue < 30) || (context.cluster has maxGeneralClassInstanceSizeValue && context.cluster.maxGeneralClassInstanceSizeValue > 60) };\"\n      }\n   ]\n}",
      "tokenEstimate": 103
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Enforcing Existence of a Project Maintenance Window\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.maintenanceWindow.modify\\\", resource) when {context.project.hasDefinedMaintenanceWindow == false};\"\n      }\n   ]\n}",
      "tokenEstimate": 68
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing Modifications to Peering Across Providers\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.vpcPeering.modify\\\", resource) when {context.project.peeringConnections == [\\\"aws:000123456789:vpc-0316c47cc923ce313:10.0.0.0/16\\\", \\\"azure:fd01aafc-b3re-2193-8497-83lp3m83a1a5:rg-name:vnet\\\", \\\"gcp:inductive-cocoa-108200:default\\\"]};\"\n      }\n   ]\n}",
      "tokenEstimate": 142
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Preventing Modifications to Private Endpoints Across Providers\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"project.privateEndpoint.modify\\\", resource) when {context.project.privateEndpoints == [\\\"aws:vpce-042d72ded1748f314\\\", \\\"azure:/subscriptions/fd01aafc-b3re-2193-8497-83lp3m83a1a5/resourceGroups/rg-name/providers/Microsoft.Network/privateEndpoints/pe-name:10.0.0.4\\\", \\\"gcp:inductive-cocoa-108200:default\\\"]};\"\n      }\n   ]\n}",
      "tokenEstimate": 145
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Restricting Cluster Connections to Minimum TLS 1.2\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.minTLSVersion == ResourcePolicy::TLSVersion::\\\"tls1_2\\\"};\"\n      }\n   ]\n}",
      "tokenEstimate": 78
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Requiring Specific TLS Cipher Suite\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\\\"custom\\\" && context.cluster.cipherSuites == [ResourcePolicy::CipherSuite::\\\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\\\"]};\"\n      }\n   ]\n}",
      "tokenEstimate": 101
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Policy Requiring Default TLS Cipher Suite\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless {context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\\\"default\\\"};\"\n      }\n   ]\n}",
      "tokenEstimate": 71
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Limit Max Disk Size to 4000GB\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096 };\"\n      }\n   ]\n}",
      "tokenEstimate": 72
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Require Replica Set Clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) unless { context.cluster.clusterType == ResourcePolicy::ClusterType::\\\"replicaset\\\" };\"\n      }\n   ]\n}",
      "tokenEstimate": 68
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Require Minimum 3 Shards\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.minShardCount < 3 && !context.cluster.isConvertingToSharded };\"\n      }\n   ]\n}",
      "tokenEstimate": 72
    },
    {
      "language": "text",
      "content": "{\n   \"name\": \"Enforce config server management mode to fixed_to_dedicated for sharded clusters\",\n   \"policies\": [\n      {\n         \"body\": \"forbid (principal, action == ResourcePolicy::Action::\\\"cluster.modify\\\", resource) when { context.cluster.clusterType == ResourcePolicy::ClusterType::\\\"sharded\\\" && context.cluster has configServerManagementMode && context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\\\"fixed_to_dedicated\\\" };\"\n      }\n   ]\n}",
      "tokenEstimate": 109
    },
    {
      "language": "text",
      "content": "forbid (\n    principal,\n    action == ResourcePolicy::Action::\"cluster.modify\",\n    resource\n)\nwhen { context.project.clustersInProject > 2 };",
      "tokenEstimate": 34
    },
    {
      "language": "text",
      "content": "forbid (\n    principal,\n    action == ResourcePolicy::Action::\"cluster.modify\",\n    resource\n)\nwhen { context.cluster.cloudProviders.contains(ResourcePolicy::CloudProvider::\"gcp\") };",
      "tokenEstimate": 40
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] };",
      "tokenEstimate": 40
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {resource in ResourcePolicy::Project::\"6217f7fff7957854e2d09179\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] };",
      "tokenEstimate": 63
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {resource in ResourcePolicy::Project::\"65dcbf5ccd12a54df59a54e6\" && resource == ResourcePolicy::Cluster::\"670968dfc0a2297ef46bc02a\" && context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"]};",
      "tokenEstimate": 88
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when {context.cluster.regions.contains(ResourcePolicy::Region::\"aws:us-east-1\")};",
      "tokenEstimate": 42
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { context.cluster.regions.contains(ResourcePolicy::Region::\"aws:us-west-1\") };",
      "tokenEstimate": 42
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { context.cluster.regions.containsAny([ResourcePolicy::Region::\"aws:us-east-1\",ResourcePolicy::Region::\"aws:us-west-1\",ResourcePolicy::Region::\"azure:westeurope\"]) };",
      "tokenEstimate": 69
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\\\"cluster.modify\\\",\n   resource\n )\n unless { [ResourcePolicy::Region::\"aws:us-east-1\", ResourcePolicy::Region::\"azure:westeurope\"].containsAll(context.cluster.regions) };",
      "tokenEstimate": 57
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource == ResourcePolicy::Cluster::\"3217e2gdf79a4c54e2d0827\"\n )\n when { context.cluster.regions.containsAny([ResourcePolicy::Region::\"aws:us-east-1\",ResourcePolicy::Region::\"aws:us-west-1\"]) };",
      "tokenEstimate": 80
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n when { context.project.ipAccessList.contains(ip(\"0.0.0.0/0\")) };",
      "tokenEstimate": 45
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n unless { [ip(\"1.2.3.4/32\"), ip(\"8.8.8.8/32\"), ip(\"4.4.4.4/32\")].containsAll(context.project.ipAccessList) };",
      "tokenEstimate": 73
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n   resource\n )\n unless { context.project.ipAccessList.isEmpty() };",
      "tokenEstimate": 34
    },
    {
      "language": "text",
      "content": " forbid(\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when { (context.cluster has minGeneralClassInstanceSizeValue && context.cluster.minGeneralClassInstanceSizeValue < 30) || (context.cluster has maxGeneralClassInstanceSizeValue && context.cluster.maxGeneralClassInstanceSizeValue > 60) };",
      "tokenEstimate": 71
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.maintenanceWindow.modify\",\n   resource\n )\n when {context.project.hasDefinedMaintenanceWindow == false};",
      "tokenEstimate": 35
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.vpcPeering.modify\",\n   resource\n )\n when {context.project.peeringConnections == [\"aws:000123456789:vpc-0316c47cc923ce313:10.0.0.0/16\", \"azure:fd01aafc-b3re-2193-8497-83lp3m83a1a5:rg-name:vnet\", \"gcp:inductive-cocoa-108200:default\"]};",
      "tokenEstimate": 109
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"project.privateEndpoint.modify\",\n   resource\n )\n when {context.project.privateEndpoints == [\"aws:vpce-042d72ded1748f314\", \"azure:/subscriptions/fd01aafc-b3re-2193-8497-83lp3m83a1a5/resourceGroups/rg-name/providers/Microsoft.Network/privateEndpoints/pe-name:10.0.0.4\", \"gcp:inductive-cocoa-108200:default\"]};",
      "tokenEstimate": 111
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n { context.cluster.minTLSVersion == ResourcePolicy::TLSVersion::\"tls1_2\" };",
      "tokenEstimate": 43
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\"custom\" &&\n   context.cluster.cipherSuites == [ResourcePolicy::CipherSuite::\"TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384\"]\n };",
      "tokenEstimate": 74
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.cipherConfigMode == ResourcePolicy::CipherConfigMode::\"default\"\n };",
      "tokenEstimate": 43
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096\n };",
      "tokenEstimate": 43
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n unless\n {\n   context.cluster.clusterType == ResourcePolicy::ClusterType::\"replicaset\"\n };",
      "tokenEstimate": 42
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster.minShardCount < 3 && !context.cluster.isConvertingToSharded\n };",
      "tokenEstimate": 46
    },
    {
      "language": "text",
      "content": " forbid (\n   principal,\n   action == ResourcePolicy::Action::\"cluster.modify\",\n   resource\n )\n when\n {\n   context.cluster.clusterType == ResourcePolicy::ClusterType::\"sharded\" &&\n   context.cluster has configServerManagementMode &&\n   context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\"fixed_to_dedicated\"\n };",
      "tokenEstimate": 74
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"forbid_more_than_two_clusters\" {\n  org_id = var.org_id\n  name   = \"forbid-more-than-two-clusters\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when { context.project.clustersInProject > 2 };\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 95
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"allow_only_aws_cloud_provider\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-aws-cloud-provider\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        unless\n          { context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"aws\"] };\n      EOF\n  }]\n}",
      "tokenEstimate": 102
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"allow_only_regions\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-regions\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n          principal,\n          action == ResourcePolicy::Action::\"cluster.modify\",\n          resource\n        ) unless {\n            [ResourcePolicy::Region::\"aws:eu-west-1\", ResourcePolicy::Region::\"aws:eu-central-1\"]\n              .containsAll(context.cluster.regions)};\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 117
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"allow_only_provider_regions\" {\n  org_id = var.org_id\n  name   = \"cluster-allow-only-provider-regions\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n          principal,\n          action == ResourcePolicy::Action::\"cluster.modify\",\n          resource\n        ) unless {\n            context.cluster.cloudProviders == [ResourcePolicy::CloudProvider::\"gcp\"] ||\n            [ResourcePolicy::Region::\"aws:eu-west-1\", ResourcePolicy::Region::\"aws:eu-central-1\"]\n              .containsAll(context.cluster.regions)};\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 137
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"forbid_project_access_anywhere\" {\n  org_id = var.org_id\n  name   = \"forbid-project-access-anywhere\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"project.ipAccessList.modify\",\n            resource\n        )\n        when {context.project.ipAccessList.contains(ip(\"0.0.0.0/0\"))};\n    EOF\n    },\n  ]\n}",
      "tokenEstimate": 106
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"limit_max_disk_size\" {\n  org_id = var.org_id\n  name   = \"limit-max-4096gb-disk\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when { context.cluster has diskSizeGB && context.cluster.diskSizeGB > 4096 };\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 101
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"require_replica_set\" {\n  org_id = var.org_id\n  name   = \"require-replica-set\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        unless { context.cluster.clusterType == ResourcePolicy::ClusterType::\"replicaset\" };\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 98
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"require_min_shards\" {\n  org_id = var.org_id\n  name   = \"require-min-3-shards\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when {\n          context.cluster.minShardCount < 3 &&\n          !context.cluster.isConvertingToSharded\n        };\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 106
    },
    {
      "language": "terraform",
      "content": "resource \"mongodbatlas_resource_policy\" \"enforce_dedicated_config_server\" {\n  org_id = var.org_id\n  name   = \"enforce-dedicated-config-server\"\n  policies = [\n    {\n      body = <<EOF\n        forbid (\n            principal,\n            action == ResourcePolicy::Action::\"cluster.modify\",\n            resource\n        )\n        when {\n          context.cluster.clusterType == ResourcePolicy::ClusterType::\"sharded\" &&\n          context.cluster has configServerManagementMode &&\n          context.cluster.configServerManagementMode != ResourcePolicy::ConfigServerManagementMode::\"fixed_to_dedicated\"\n        };\n      EOF\n    },\n  ]\n}",
      "tokenEstimate": 137
    }
  ]
}
