{
  "heading": "Download the Evaluation Dataset",
  "headingLevel": 2,
  "body": "Download the [ragas-wikiqa](https://huggingface.co/datasets/explodinggradients/ragas-wikiqa)\ndataset from Hugging Face and convert it to a pandas dataframe:\n\n```python\nfrom datasets import load_dataset\nimport pandas as pd\n\ndata = load_dataset(\"explodinggradients/ragas-wikiqa\", split=\"train\")\ndf = pd.DataFrame(data)\n```\n\nThe dataset contains the following columns:\n\n- `question`: User questions\n\n- `correct_answer`: Ground truth answers\n\n- `context`: List of reference texts to answer the questions",
  "codeBlocks": [
    {
      "language": "python",
      "content": "from datasets import load_dataset\nimport pandas as pd\n\ndata = load_dataset(\"explodinggradients/ragas-wikiqa\", split=\"train\")\ndf = pd.DataFrame(data)",
      "tokenEstimate": 37
    }
  ]
}
