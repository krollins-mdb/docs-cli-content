{
  "heading": "Create the Indexes",
  "headingLevel": 2,
  "body": "To enable hybrid search queries on your vector store,\ncreate a MongoDB Vector Search and MongoDB Search index on the collection.\nYou can create the indexes by using either the\nLangChain helper methods or the\n[PyMongo Driver](pymongo) method:**Langchain:**LangChainRun the following code to create\na [index definition](avs-types-vector-search) that indexes the\n`plot_embedding_voyage_3_large` field in the collection.```python\n# Use helper method to create the vector search index\nvector_store.create_vector_search_index(\n   dimensions = 2048 # The dimensions of the vector embeddings to be indexed\n)\n```> **Tip:** [create\\_vector\\_search\\_index API Reference](https://langchain-mongodb.readthedocs.io/en/latest/langchain_mongodb/index/langchain_mongodb.index.create_vector_search_index.html)Run the following code in your notebook to create a\n[MongoDB Search indexes](fts-manage-indexes)\nthat indexes the `plot` field in the collection.```python\nfrom langchain_mongodb.index import create_fulltext_search_index\nfrom pymongo import MongoClient\n\n# Connect to your cluster\nclient = MongoClient(MONGODB_URI)\n\n# Use helper method to create the search index\ncreate_fulltext_search_index(\n   collection = client[\"sample_mflix\"][\"embedded_movies\"],\n   field = \"plot\",\n   index_name = \"search_index\"\n)\n```> **Tip:** [create\\_fulltext\\_search\\_index API Reference](https://langchain-mongodb.readthedocs.io/en/latest/langchain_mongodb/index/langchain_mongodb.index.create_fulltext_search_index.html)**Pymongo:**PyMongoRun the following code to create\na [index definition](avs-types-vector-search) that indexes the\n`plot_embedding_voyage_3_large` field in the collection.```python\nfrom pymongo import MongoClient\nfrom pymongo.operations import SearchIndexModel\n\n# Connect to your cluster\nclient = MongoClient(MONGODB_URI)\ncollection = client[\"sample_mflix\"][\"embedded_movies\"]\n\n# Create your vector search index model, then create the index\nvector_index_model = SearchIndexModel(\n   definition={\n      \"fields\": [\n         {\n         \"type\": \"vector\",\n         \"path\": \"plot_embedding_voyage_3_large\",\n         \"numDimensions\": 2048,\n         \"similarity\": \"dotProduct\"\n         }\n      ]\n   },\n   name=\"vector_index\",\n   type=\"vectorSearch\"\n)\ncollection.create_search_index(model=vector_index_model)\n```Run the following code to create a\n[MongoDB Search indexes](fts-manage-indexes)\nthat indexes the `plot` field in the collection.```python\n# Create your search index model, then create the search index\nsearch_index_model = SearchIndexModel(\n   definition={\n      \"mappings\": {\n            \"dynamic\": False,\n            \"fields\": {\n               \"plot\": {\n                  \"type\": \"string\"\n               }\n            }\n      }\n   },\n   name=\"search_index\"\n)\ncollection.create_search_index(model=search_index_model)\n```The indexes should take about one minute to build. While they build,\nthe indexes are in an [initial sync process](troubleshoot-initial-sync)\nstate. When they finish building, you can start querying the data\nin your collection.",
  "codeBlocks": [
    {
      "language": "python",
      "content": "# Use helper method to create the vector search index\nvector_store.create_vector_search_index(\n   dimensions = 2048 # The dimensions of the vector embeddings to be indexed\n)",
      "tokenEstimate": 36
    },
    {
      "language": "python",
      "content": "from langchain_mongodb.index import create_fulltext_search_index\nfrom pymongo import MongoClient\n\n# Connect to your cluster\nclient = MongoClient(MONGODB_URI)\n\n# Use helper method to create the search index\ncreate_fulltext_search_index(\n   collection = client[\"sample_mflix\"][\"embedded_movies\"],\n   field = \"plot\",\n   index_name = \"search_index\"\n)",
      "tokenEstimate": 74
    },
    {
      "language": "python",
      "content": "from pymongo import MongoClient\nfrom pymongo.operations import SearchIndexModel\n\n# Connect to your cluster\nclient = MongoClient(MONGODB_URI)\ncollection = client[\"sample_mflix\"][\"embedded_movies\"]\n\n# Create your vector search index model, then create the index\nvector_index_model = SearchIndexModel(\n   definition={\n      \"fields\": [\n         {\n         \"type\": \"vector\",\n         \"path\": \"plot_embedding_voyage_3_large\",\n         \"numDimensions\": 2048,\n         \"similarity\": \"dotProduct\"\n         }\n      ]\n   },\n   name=\"vector_index\",\n   type=\"vectorSearch\"\n)\ncollection.create_search_index(model=vector_index_model)",
      "tokenEstimate": 136
    },
    {
      "language": "python",
      "content": "# Create your search index model, then create the search index\nsearch_index_model = SearchIndexModel(\n   definition={\n      \"mappings\": {\n            \"dynamic\": False,\n            \"fields\": {\n               \"plot\": {\n                  \"type\": \"string\"\n               }\n            }\n      }\n   },\n   name=\"search_index\"\n)\ncollection.create_search_index(model=search_index_model)",
      "tokenEstimate": 77
    }
  ]
}
