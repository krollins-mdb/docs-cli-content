{
  "heading": "Step 3: Instantiate the graph store.",
  "headingLevel": 3,
  "body": "Use the `MongoDBGraphStore`\nclass to construct the knowledge graph and load it into your\nMongoDB cluster:\n\n```python\nfrom langchain_mongodb.graphrag.graph import MongoDBGraphStore\n\ngraph_store = MongoDBGraphStore.from_connection_string(\n    connection_string = MONGODB_URI,\n    database_name = DB_NAME,\n    collection_name = COLLECTION,\n    entity_extraction_model = chat_model\n)\n```",
  "codeBlocks": [
    {
      "language": "python",
      "content": "from langchain_mongodb.graphrag.graph import MongoDBGraphStore\n\ngraph_store = MongoDBGraphStore.from_connection_string(\n    connection_string = MONGODB_URI,\n    database_name = DB_NAME,\n    collection_name = COLLECTION,\n    entity_extraction_model = chat_model\n)",
      "tokenEstimate": 53
    }
  ],
  "slug": "step-3-instantiate-the-graph-store",
  "variantAgnostic": true
}
