{
  "heading": "shingle",
  "headingLevel": 2,
  "body": "The `shingle` token filter constructs shingles (token n-grams) from a\nseries of tokens. You can't use the `shingle` token filter in\n[Synonym](synonyms-ref) or [tokenization](bson-data-types-autocomplete) mapping definitions.\n\n> **Note:**&#x20;\n>\n> For querying with [regex](regex-ref) or [wildcard](wildcard-ref), you can't use\n> wordDelimiterGraph token filter as the `searchAnalyzer` as it produces more than one\n> output token per input token. Specify a different analyzer as the\n> `searchAnalyzer` in your index definition.",
  "codeBlocks": [],
  "slug": "shingle",
  "variantAgnostic": true
}
