{
  "heading": "keywordRepeat",
  "headingLevel": 2,
  "body": "The `keywordRepeat` token filter emits each incoming token twice, as a\nkeyword and as a non-keyword. You can stem the non-keyword token using\nsubsequent token filters and preserve the keyword token. This can be\nused to boost exact matches and retrieve stemmed matches. This is\ntypically used in conjunction with a stemming filter, such as\n[porterStemming](porterStemming-tf-ref), followed by [removeDuplicates](removeDuplicates-tf-ref).",
  "codeBlocks": []
}
