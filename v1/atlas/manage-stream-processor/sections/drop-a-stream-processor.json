{
  "heading": "Drop a Stream Processor",
  "headingLevel": 2,
  "body": "To drop a stream processor:**Admin-api:**Atlas Administration APIThe Atlas Administration API provides an endpoint for\ndeleting a stream processor.[Delete One Stream Processor](api/doc/atlas-admin-api-v2/operation/operation-deletegroupstreamprocessor)**Atlas UI:**Atlas UITo delete a stream processor in the Atlas UI, go to the\n**Stream Processing** page for your Atlas project\nand click **Manage** in the pane for your stream processing workspace to\nview the list of stream processors defined for it.Then, click the **Delete** (trash-alt) icon for\nyour stream processor. In the confirmation dialog that appears,\ntype the name of the stream processor (`solarDemo`) to confirm\nthat you want to delete it, and then click **Delete**.**mongosh:**`mongosh`To delete an existing stream processor with [mongosh](mongodb-shell), use the\n[sp.processor.drop()](manual/reference/method/sp.processor.drop) method. It has the following syntax:```sh\nsp.processor.drop(<options>)\n```Where `<options>` is a generic, optional document whose fields are\npassed to the underlying drop command.For example, to drop a stream processor named `proc01`, run the\nfollowing command:```sh\nsp.proc01.drop()\n```This method returns:* `true` if the stream processor exists.\n\n* `false` if the stream processor doesn't exist.When you drop a stream processor, all resources that Atlas Stream Processing\nprovisioned for it are destroyed, along with all saved state.",
  "codeBlocks": [
    {
      "language": "sh",
      "content": "sp.processor.drop(<options>)",
      "tokenEstimate": 6
    },
    {
      "language": "sh",
      "content": "sp.proc01.drop()",
      "tokenEstimate": 5
    }
  ]
}
