{
  "heading": "Stop a Stream Processor",
  "headingLevel": 2,
  "body": "> **Note:** Atlas Stream Processing discards the internal state of stream processors which\n> have been `stopped` for 45 days or more. When you start such a\n> processor, it operates and reports [statistics](atlas-sp-manage-processor-stats) identically to its initial run.\n\nTo stop a stream processor:\n\n**Admin-api:**\n\nAtlas Administration API\n\nThe Atlas Administration API provides an endpoint for\nstopping a stream processor.\n\n[Stop One Stream Processor](api/doc/atlas-admin-api-v2/operation/operation-stopgroupstreamprocessor)\n\n**Atlas UI:**\n\nAtlas UI\n\nTo stop a stream processor in the Atlas UI, go to the\n**Stream Processing** page for your Atlas project\nand click **Manage** in the pane for your stream processing workspace to\nview the list of stream processors defined for it.\n\nThen, click the **Stop** icon for your stream processor.\n\n**mongosh:**\n\n`mongosh`\n\nTo stop an existing stream processor with [mongosh](mongodb-shell), use the\n[sp.processor.stop()](manual/reference/method/sp.processor.stop) method. It has the following syntax:\n\n```sh\nsp.processor.stop(<options>)\n```\n\nWhere `<options>` is a generic, optional document whose fields are\npassed to the underlying stop command.\n\nFor example, to stop a stream processor named `proc01`, run the\nfollowing command:\n\n**Input:**\n\n```shell\nsp.proc01.stop()\n```\n\n**Output:**\n\n```shell\n{ \"ok\" : 1 }\n```\n\nThis method returns `{ \"ok\": 1 }` if the stream processor exists\nand is currently running. If you invoke `sp.processor.stop()`\nfor a stream processor that is not `running`, [mongosh](mongodb-shell) will\nreturn an error.",
  "codeBlocks": [
    {
      "language": "sh",
      "content": "sp.processor.stop(<options>)",
      "tokenEstimate": 6
    },
    {
      "language": "shell",
      "content": "sp.proc01.stop()",
      "tokenEstimate": 5
    },
    {
      "language": "shell",
      "content": "{ \"ok\" : 1 }",
      "tokenEstimate": 8
    }
  ]
}
