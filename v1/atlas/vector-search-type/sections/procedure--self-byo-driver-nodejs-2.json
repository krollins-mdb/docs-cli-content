{
  "heading": "Procedure",
  "headingLevel": 3,
  "body": "To view a MongoDB Vector Search index for a collection using\n[MongoDB Node driver](node/fundamentals/indexes)\nv6.6.0 or later, perform the following steps:\n\n```javascript\nconst { MongoClient } = require(\"mongodb\");\n\n// connect to your deployment\nconst uri =  \"<connectionString>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n\n    // run the helper method\n    const result = await collection.listSearchIndexes(\"<indexName>\").toArray();\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n\n```\n\n| \\<connectionString> | Cluster connection string. To learn more, see Connect to a Cluster via Client Libraries.                                   |\n| ------------------- | -------------------------------------------------------------------------------------------------------------------------- |\n| \\<databaseName>     | The database that contains the collection.                                                                                 |\n| \\<collectionName>   | The collection for which you want to retrieve the indexes.                                                                 |\n| \\<indexName>        | The name of your index if you want to retrieve a specific index. To return all indexes on the collection, omit this value. |\n\n```shell\nnode <file-name>.js\n```",
  "codeBlocks": [
    {
      "language": "javascript",
      "content": "const { MongoClient } = require(\"mongodb\");\n\n// connect to your deployment\nconst uri =  \"<connectionString>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n    const database = client.db(\"<databaseName>\");\n    const collection = database.collection(\"<collectionName>\");\n\n    // run the helper method\n    const result = await collection.listSearchIndexes(\"<indexName>\").toArray();\n    console.log(result);\n  } finally {\n    await client.close();\n  }\n}\nrun().catch(console.dir);\n",
      "tokenEstimate": 103
    },
    {
      "language": "shell",
      "content": "node <file-name>.js",
      "tokenEstimate": 6
    }
  ],
  "slug": "procedure--self-byo-driver-nodejs",
  "selections": {
    "deployment-type": "self",
    "embedding": "byo",
    "interface": "driver",
    "language": "nodejs"
  },
  "variantAgnostic": false
}
