{
  "heading": "Step 2: Create the HTTPS connection.",
  "headingLevel": 2,
  "body": "Run the following Atlas CLI command to create the connection:\n\n```sh\natlas streams connections create -i <instance-name> -f </path/to/config.json>\n```\n\nTo add an S3 (Simple Storage Service) connection to your stream processing workspace through\nAtlas CLI, follow these steps:",
  "codeBlocks": [
    {
      "language": "sh",
      "content": "atlas streams connections create -i <instance-name> -f </path/to/config.json>",
      "tokenEstimate": 19
    }
  ],
  "slug": "step-2-create-the-https-connection--atlas-cli-https",
  "selections": {
    "interface": "atlas-cli",
    "sp-connections": "https"
  },
  "variantAgnostic": false
}
