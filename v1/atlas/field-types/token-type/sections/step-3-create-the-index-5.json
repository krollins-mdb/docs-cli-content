{
  "heading": "Step 3: Create the index.",
  "headingLevel": 2,
  "body": "**Input:**```shell\nnode create-index.js\n```**Output:**```\nNew index name: default\n```The MongoDB Search dateFacet type takes the following parameters:| Option     | Type   | Necessity | Description                                                                                                                                                                                                                                                       | Default |\n| ---------- | ------ | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |\n| type       | string | Required  | Human-readable label that identifies this field type. Value must be token.                                                                                                                                                                                        |         |\n| normalizer | string | Optional  | Type of transformation to perform on the field value. Value can be one of the following:lowercase - to transform text values in string fields to lowercase.none - to not perform any transformation.If you don't set this option explicitly, it defaults to none. | none    |The following index definition examples use collections in the\n[sample data sets](available-sample-datasets). After you\n[Load Sample Data Into Atlas](load-sample-data), you can use\nthese examples to index fields as the dateFacet type.To learn how to set up and run the following example code, see\nDefine the Index for the token Type.Index as token Type Only ExampleThe following index definition indexes string values in the\n`title` field as MongoDB Search dateFacet type and converts the\nfield value to `lowercase`, which allows you to do the\nfollowing:* Perform case-insensitive sort, as specified by the\n  `normalizer`, on the `title` field.\n\n* Run exact match queries on the `title` field using the\n  following operators:\n\n  - [equals](equals-ref)\n\n  - [in](in-ref)\n\n  - [range](range-ref)```javascript\nconst { MongoClient } = require(\"mongodb\");\n\n// connect to your Atlas deployment\nconst uri =  \n  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n\n    // set namespace\n    const database = client.db(\"<database>\");\n    const collection = database.collection(\"<collection>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"default\",\n        definition: {\n            /* search index definition fields */\n            \"mappings\": {\n                \"dynamic\": false,\n                \"fields\": {\n                    \"title\": {\n                        \"type\": \"token\",\n                        \"normalizer\": \"lowercase\"\n                    }\n                }\n            }\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(\"New index name: \" + result);\n  } finally {\n    await client.close();\n  }\n}\n\nrun().catch(console.dir);\n```Index as Multiple Types ExampleThe following index definition indexes the `genres` field as\n`string` and `token` types to return the following:* Search results for queries using MongoDB Search operators like\n  [text](text-ref), [Phrase](phrase-ref), and other operators that\n  perform text search on the `genres` field.\n\n* Sorted results for queries using the [$search](pipe.$search)\n  [sorting](sort-ref) option on the `genres` field.\n\n* Exact matches for queries using MongoDB Search operators like\n  [equals](equals-ref), [in](in-ref), and [range](range-ref).```javascript\nconst { MongoClient } = require(\"mongodb\");\n\n// connect to your Atlas deployment\nconst uri =  \n  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n\n    // set namespace\n    const database = client.db(\"<database>\");\n    const collection = database.collection(\"<collection>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"default\",\n        definition: {\n            /* search index definition fields */\n            \"mappings\": {\n                \"dynamic\": false,\n                \"fields\": {\n                    \"genres\": [\n                        {\n                            \"type\": \"string\"\n                        },\n                        {\n                            \"type\": \"token\"\n                        }\n                    ]\n                }\n            }\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(\"New index name: \" + result);\n  } finally {\n    await client.close();\n  }\n}\n\nrun().catch(console.dir);\n```You must use the MongoDB Search `token` type to index [string](manual/reference/bson-types) fields to enable sorting and faceting\non these fields. Then, you can perform the following actions:* Use the `sort` option in your query\n  to sort the results by the indexed field. To learn more, see\n  [sorting](sort-ref).\n\n* Use the `facet` collector in your query to group the results by the\n  indexed field. To learn more, see [How to Use Facets with MongoDB Search](facet-tutorial).Additionally, you must index string fields as the `token` type\nin order to use the [equals](equals-ref), [in](in-ref)\nand [range](range-ref) operators. To learn more, see the\ndocumentation for each respective [MongoDB Search operators](operators-ref).> **Note:** You can also use the `token` type to index\n> string fields for pre-filtering your data for [$vectorSearch](pipe.$vectorSearch)\n> queries. To learn more, see [MongoDB Vector Search](fts-vector-search).When you index a field as the `token` type, you must index that field\nas `string` type also to query the text value using operators such as\n[text](text-ref), [Phrase](phrase-ref), etc. For the following operators,\nyou don't need to index the field as `string` type also to query the\ntext value in the field:- [equals](equals-ref)\n\n- [in](in-ref)\n\n- [range](range-ref)> **Note:** We do not recommend indexing a field with both the `stringFacet` (deprecated)\n> and `token` types, unless you require a different normalizer for the `token`\n> type for a use case other than faceting. With this index definition, MongoDB Search\n> uses the `stringFacet` definition for faceting.When you index a field as `token` type, MongoDB Search indexes the terms in\nthe string as a single token (searchable term) and stores them in a\ncolumnar storage for efficient filtering or sort operations. You\ncan use a [normalizer](ref-built-in-normalizers) to transform the\ntoken. By default, the normalizer is set to `none` and so MongoDB Search\nindexes strings in their original form.The major difference between the MongoDB Search `string` and `token` types is\nthat MongoDB Search creates one or more tokens for fields indexed as `string`\ntype whereas MongoDB Search creates only a single token for fields indexed as the\n`token` type.If a string being indexed as a `token` field type exceeds 8181\ncharacters, MongoDB Search truncates it to 8181 characters before indexing.",
  "codeBlocks": [
    {
      "language": "shell",
      "content": "node create-index.js",
      "tokenEstimate": 4
    },
    {
      "language": "text",
      "content": "New index name: default",
      "tokenEstimate": 5
    },
    {
      "language": "javascript",
      "content": "const { MongoClient } = require(\"mongodb\");\n\n// connect to your Atlas deployment\nconst uri =  \n  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n\n    // set namespace\n    const database = client.db(\"<database>\");\n    const collection = database.collection(\"<collection>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"default\",\n        definition: {\n            /* search index definition fields */\n            \"mappings\": {\n                \"dynamic\": false,\n                \"fields\": {\n                    \"title\": {\n                        \"type\": \"token\",\n                        \"normalizer\": \"lowercase\"\n                    }\n                }\n            }\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(\"New index name: \" + result);\n  } finally {\n    await client.close();\n  }\n}\n\nrun().catch(console.dir);",
      "tokenEstimate": 190
    },
    {
      "language": "javascript",
      "content": "const { MongoClient } = require(\"mongodb\");\n\n// connect to your Atlas deployment\nconst uri =  \n  \"<connection-string>\";\n\nconst client = new MongoClient(uri);\n\nasync function run() {\n  try {\n\n    // set namespace\n    const database = client.db(\"<database>\");\n    const collection = database.collection(\"<collection>\");\n    \n    // define your MongoDB Search index\n    const index = {\n        name: \"default\",\n        definition: {\n            /* search index definition fields */\n            \"mappings\": {\n                \"dynamic\": false,\n                \"fields\": {\n                    \"genres\": [\n                        {\n                            \"type\": \"string\"\n                        },\n                        {\n                            \"type\": \"token\"\n                        }\n                    ]\n                }\n            }\n        }\n    }\n\n    // run the helper method\n    const result = await collection.createSearchIndex(index);\n    console.log(\"New index name: \" + result);\n  } finally {\n    await client.close();\n  }\n}\n\nrun().catch(console.dir);",
      "tokenEstimate": 196
    }
  ]
}
